# ثبت تصمیم‌های اجرایی OrgaWork

## 1. شناسنامه سند

- شناسه سند: `DECISIONS`
- نسخه بازبینی‌شده: `0.2.0`
- وضعیت: مرجع تصمیم‌های اجرایی الزام‌آور
- تاریخ بازبینی فنی: `2026-07-28`
- شاخه مرجع: `main`
- مرجع تصمیم‌های معماری تفصیلی: `APPROVED-BASELINES.md`
- مرجع وضعیت جاری: `PROJECT-STATUS.md`
- مرجع ترتیب اجرا: `ROADMAP.md`
- مرجع تاریخچه اجرا: `IMPLEMENTATION-JOURNAL.md`

این سند تصمیم‌های کوتاه، قطعی و قابل‌اجرای پروژه را ثبت می‌کند.
جزئیات معماری هر تصمیم ممکن است در ADR، مشخصات محصول یا سند تخصصی مرحله نگهداری شود.

## 2. قواعد ثبت تصمیم

هر تصمیم باید تا حد امکان شامل موارد زیر باشد:

- شناسه یکتا؛
- عنوان روشن؛
- وضعیت؛
- دامنه اثر؛
- متن تصمیم؛
- دلیل؛
- پیامد اجرایی؛
- شرایط تغییر یا جایگزینی.

وضعیت‌های معتبر:

- `پذیرفته‌شده`
- `جایگزین‌شده`
- `لغوشده`

قواعد:

1. تصمیم پذیرفته‌شده تا زمان ثبت تصمیم جایگزین معتبر است.
2. تاریخچه تصمیم حذف نمی‌شود.
3. تغییر رفتار بدون به‌روزرسانی تصمیم مرتبط مجاز نیست.
4. تصمیم باز یا پیشنهادشده نباید در این فایل به‌صورت تصمیم قطعی ثبت شود.
5. جزئیات ثبت‌نشده نباید از روی حافظه یا بسته آرشیوی حدس زده شوند.
6. تصمیم‌های این سند باید با کد، نقشه راه و اسناد تخصصی سازگار باشند.

# تصمیم‌های پذیرفته‌شده

## DEC-001 — زبان رابط عمومی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام صفحات، مؤلفه‌ها، پیام‌ها، اعلان‌ها، گزارش‌ها و فایل‌های قابل مشاهده برای کاربر نهایی

### تصمیم

تمام متن‌های قابل مشاهده برای کاربر نهایی باید فارسی روان، روشن و متناسب با زمینه محصول باشند.

نمایش موارد زیر در رابط عمومی مجاز نیست:

- واژه انگلیسی خام؛
- نام فنی کلاس، تابع یا متغیر؛
- کد خطای کتابخانه؛
- Stack Trace؛
- پیام خام سرویس یا Provider؛
- شناسه فنی بدون توضیح فارسی.

استفاده از واژه یا شناسه انگلیسی در موارد زیر مجاز است:

- کد؛
- Contract فنی؛
- OpenAPI؛
- گزارش توسعه؛
- رخداد فنی؛
- مستندات فنی؛
- نام بسته و وابستگی.

### دلیل

کاربران هدف فارسی‌زبان هستند و رابط باید بدون نیاز به دانش فنی قابل استفاده باشد.

### پیامد اجرایی

- سند HTML باید `lang="fa"` داشته باشد.
- رابط باید `dir="rtl"` داشته باشد.
- متن‌های عمومی باید از منبع مرکزی قابل کنترل باشند.
- آزمون جلوگیری از نمایش ناخواسته واژه انگلیسی باید حفظ و گسترش یابد.
- پیام خطای فنی باید پیش از نمایش به پیام فارسی پایدار نگاشت شود.

### شرایط تغییر

تغییر زبان عمومی یا افزودن رابط چندزبانه نیازمند تصمیم مستقل، طراحی ترجمه و آزمون کامل RTL و LTR است.

## DEC-002 — تقویم، تاریخ و زمان

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: رابط کاربری، قراردادها، پایگاه داده، صف، رخدادها، گزارش‌ها و فایل‌های خروجی

### تصمیم

- تمام تاریخ‌های قابل مشاهده و ورودی عمومی باید بر پایه تقویم هجری شمسی باشند.
- انتخابگرهای تاریخ عمومی باید شمسی باشند.
- زمان ذخیره، انتقال، صف، رخداد فنی و Audit باید UTC و با قالب استاندارد ISO باشد.
- منطقه زمانی کاربر یا سازمان باید هنگام تبدیل برای نمایش لحاظ شود.
- زمان محلی دستگاه کاربر یا سرور منبع حقیقت نیست.

### دلیل

نمایش شمسی نیاز محصول است، درحالی‌که ذخیره UTC برای سازگاری، ترتیب زمانی و پردازش چندمنطقه‌ای ضروری است.

### پیامد اجرایی

- تبدیل تاریخ در مرز رابط انجام می‌شود.
- رشته تاریخ شمسی به‌عنوان زمان نهایی پایدار ذخیره نمی‌شود.
- Offset ثابت جایگزین منطقه زمانی IANA نیست.
- آزمون تاریخ نامعتبر، منطقه زمانی خالی و مرزهای تبدیل ضروری است.
- خروجی‌های PDF، XLSX، CSV و اعلان‌های عمومی نیز مشمول این تصمیم هستند.

### شرایط تغییر

تغییر تقویم عمومی یا روش ذخیره زمان نیازمند تصمیم معماری، برنامه مهاجرت و آزمون داده تاریخی است.

## DEC-003 — روش همکاری و توسعه مرحله‌ای

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: همکاری توسعه در محیط فعلی Windows و PowerShell

### تصمیم

توسعه به‌صورت مرحله‌به‌مرحله انجام می‌شود:

1. در هر نوبت فقط یک فرمان PowerShell ارائه می‌شود.
2. هدف فرمان کوتاه توضیح داده می‌شود.
3. فرمان باید پیش‌شرط‌ها و دامنه تغییر را بررسی کند.
4. خروجی کامل کاربر دریافت می‌شود.
5. موفقیت تا پیش از بررسی خروجی فرض نمی‌شود.
6. سپس فقط گام بعدی ارائه می‌شود.

### دلیل

این روش احتمال اجرای فرمان در مسیر نادرست، بازنویسی فایل، ثبت تغییر ناخواسته و ادامه بر اساس فرض را کاهش می‌دهد.

### پیامد اجرایی

- فرمان‌های تغییردهنده در یک Block با `$ErrorActionPreference = 'Stop'` اجرا می‌شوند.
- مسیر پروژه به‌صورت صریح تنظیم می‌شود.
- شاخه و وضعیت Git پیش از تغییر بررسی می‌شوند.
- فایل ناشناخته حذف یا بازنشانی نمی‌شود.
- Commit و Tag پیش از پذیرش ایجاد نمی‌شوند.

### شرایط تغییر

تغییر این روش فقط با درخواست صریح کاربر یا انتقال رسمی به گردش کار خودکار پذیرفته‌شده مجاز است.

## DEC-004 — مخزن، منبع دائمی حقیقت پروژه

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام مراحل، اسناد، تصمیم‌ها و ادعاهای پیشرفت

### تصمیم

مخزن جاری در شاخه `main`، همراه با کد، تنظیمات، Migrationها، اسناد، Commitها، Tagها و شواهد آزمون، منبع دائمی حقیقت پروژه است.

گفت‌وگوها، حافظه دستیار، فایل موقت و خروجی خارج از مخزن به‌تنهایی منبع حقیقت نیستند.

### دلیل

ادامه پروژه نباید به دسترسی به یک گفت‌وگو یا حافظه اشخاص وابسته باشد.

### پیامد اجرایی

- تصمیم و قرارداد لازم باید در مخزن ثبت شود.
- وضعیت جاری باید در `PROJECT-STATUS.md` ثبت شود.
- مسیر آینده باید در `ROADMAP.md` ثبت شود.
- اجرای واقعی باید در `IMPLEMENTATION-JOURNAL.md` ثبت شود.
- ادعای تکمیل بدون شاهد مخزن پذیرفته نیست.

### شرایط تغییر

این تصمیم بنیادی است و فقط با تصمیم رسمی درباره سامانه جایگزین مدیریت منبع حقیقت قابل تغییر است.

## DEC-005 — بسته‌های آرشیوی و نمونه‌ها غیرالزام‌آور هستند

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام فایل‌ها، بسته‌ها، پروژه‌های قدیمی، نمونه‌ها و Runtimeهای خارج از مخزن جاری

### تصمیم

بسته آرشیوی، نسخه قدیمی پروژه، Runtime حافظه‌ای یا نمونه مشابه فقط برای مقایسه، استخراج نیازمندی و طراحی آزمون قابل استفاده است.

هیچ‌یک از موارد زیر نباید بدون ممیزی و تصمیم به مخزن جاری منتقل شوند:

- پشته فنی؛
- نسخه پایگاه داده؛
- Schema؛
- Migration؛
- Endpoint؛
- Payload؛
- کد خطا؛
- مدل مجوز؛
- وضعیت دامنه؛
- ادعای تکمیل قابلیت.

### دلیل

بسته‌های آرشیوی بررسی‌شده دارای تفاوت‌هایی در پشته و سطح پیاده‌سازی بوده‌اند و نمی‌توانند مرجع وضعیت جاری باشند.

### پیامد اجرایی

هر انتقال از آرشیو باید:

1. با تصمیم‌ها و خط‌مبناهای فعلی تطبیق داده شود؛
2. با پشته جاری سازگار شود؛
3. امنیت چندسازمانی آن بررسی شود؛
4. آزمون بازگشتی داشته باشد؛
5. در Journal ثبت شود.

### شرایط تغییر

هیچ بسته‌ای بدون تصویب رسمی نمی‌تواند به منبع حقیقت ارتقا یابد.

## DEC-006 — منع حدس‌زدن قرارداد و رفتار

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: دامنه، پایگاه داده، API، صف، UI، فایل، گزارش و زیرساخت

### تصمیم

اگر جزئیات دقیق یک موضوع در اسناد یا کد پذیرفته‌شده وجود ندارد، نباید از روی حافظه، نام فایل، عرف عمومی یا بسته آرشیوی ساخته شود.

موارد مشمول:

- نام جدول و ستون؛
- نوع ستون؛
- Endpoint؛
- Payload؛
- کد خطا؛
- انتقال وضعیت؛
- ساختار رویداد؛
- قرارداد Queue؛
- سیاست Retry؛
- محدودیت فایل؛
- مدت نگهداری؛
- Provider؛
- متن نهایی رابط.

### دلیل

حدس‌زدن موجب Drift میان دامنه، داده، قرارداد، رابط و آزمون می‌شود.

### پیامد اجرایی

پیش از پیاده‌سازی جزئیات ثبت‌نشده باید:

1. مسئله مشخص شود؛
2. گزینه‌ها بررسی شوند؛
3. Contract یا تصمیم نوشته شود؛
4. اثر امنیتی و مهاجرت بررسی شود؛
5. آزمون تعریف شود؛
6. سپس پیاده‌سازی آغاز شود.

### شرایط تغییر

این تصمیم قابل تعلیق نیست. فقط جزئیات موضوع می‌تواند با قرارداد رسمی تعیین شود.

## DEC-007 — معماری یکپارچه ماژولار و چهار برنامه اجرایی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: ساختار مخزن، وابستگی‌ها، Build، اجرا و استقرار

### تصمیم

معماری نسخه نخست یکپارچه ماژولار است و چهار برنامه اجرایی مستقل دارد:

- Web؛
- API؛
- Worker؛
- Scheduler.

### دلیل

این ساختار مرز دامنه را حفظ می‌کند و درعین‌حال پیچیدگی عملیاتی Microservices را در نسخه نخست تحمیل نمی‌کند.

### پیامد اجرایی

- منطق دامنه در ماژول‌های مستقل نگهداری می‌شود.
- برنامه‌های اجرایی نباید مالک قواعد نهایی دامنه باشند.
- ماژول دامنه نباید به برنامه اجرایی وابسته باشد.
- وابستگی چرخه‌ای ممنوع است.
- چهار برنامه باید مستقل ساخته و اجرا شوند.
- ارتباط میان مرزها باید قرارداد روشن داشته باشد.

### شرایط تغییر

تبدیل معماری به Microservices یا Event Sourcing کامل نیازمند ADR جدید، تحلیل هزینه و برنامه مهاجرت است.

## DEC-008 — پشته فنی مرجع مرحله P1

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: توسعه پایه، وابستگی‌ها، زیرساخت و قراردادها

### تصمیم

پشته مرجع فعلی شامل موارد زیر است:

- Node.js نسل 24؛
- TypeScript نسل 6؛
- Next.js برای Web؛
- Fastify برای API؛
- pnpm Workspace؛
- Turborepo؛
- Vitest؛
- PostgreSQL 16؛
- Redis 7؛
- MinIO یا ذخیره‌ساز سازگار با S3؛
- Docker Compose؛
- OpenAPI؛
- Playwright در مرحله آزمون مرورگر.

نسخه واقعی وابستگی‌های نصب‌شده از `package.json`ها و `pnpm-lock.yaml` خوانده می‌شود.

### دلیل

پشته جاری در مخزن راه‌اندازی و برای مراحل بسته‌شده آزموده شده است.

### پیامد اجرایی

- تغییر Fastify به چارچوب دیگر مجاز نیست مگر با تصمیم جایگزین.
- نسخه PostgreSQL 18 موجود در برخی منابع آرشیوی مرجع پروژه جاری نیست.
- چارچوب NestJS یا ORM موجود در نمونه آرشیوی به‌صورت خودکار وارد پروژه نمی‌شود.
- ارتقای عمده نسخه باید با آزمون کامل انجام شود.

### شرایط تغییر

هر تغییر اصلی پشته نیازمند تصمیم مستقل، بررسی سازگاری، Build تازه، آزمون کامل و برنامه مهاجرت است.

## DEC-009 — منبع نهایی داده و نقش سرویس‌های ذخیره‌سازی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: داده پایدار، Queue، Cache، فایل و بازیابی

### تصمیم

- PostgreSQL منبع نهایی و معتبر وضعیت پایدار تجاری است.
- Redis برای Queue، Lease، هماهنگی کوتاه‌مدت و داده زودگذر استفاده می‌شود.
- Redis منبع نهایی حقیقت تجاری نیست.
- بایت فایل در ذخیره‌ساز سازگار با S3 نگهداری می‌شود.
- فراداده و وضعیت فایل در PostgreSQL ثبت می‌شود.

### دلیل

این تفکیک امکان بازیابی، کنترل تراکنش و جلوگیری از ازدست‌رفتن وضعیت پایدار را فراهم می‌کند.

### پیامد اجرایی

- شکست Redis نباید وضعیت اصلی کسب‌وکار را حذف کند.
- Queue باید از وضعیت پایدار قابل بازیابی باشد.
- Bucket فایل به‌صورت پیش‌فرض خصوصی است.
- دسترسی فایل فقط بر اساس وجود Object در ذخیره‌ساز تعیین نمی‌شود.
- Backup و Restore پایگاه داده و فایل باید جداگانه طراحی و باهم اعتبارسنجی شوند.

### شرایط تغییر

تغییر منبع نهایی داده نیازمند ADR، Migration داده و آزمون بازیابی کامل است.

## DEC-010 — جداسازی سخت‌گیرانه سازمانی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام داده‌های سازمانی، API، Worker، Scheduler، جست‌وجو، گزارش و فایل

### تصمیم

هر داده سازمانی باید سازمان مالک مشخص داشته باشد و دسترسی میان سازمان‌ها به‌طور سخت‌گیرانه ممنوع است.

کنترل جداسازی فقط در UI یا API کافی نیست و باید در PostgreSQL با سیاست‌های مناسب، از جمله RLS، نیز اعمال شود.

### دلیل

محصول چندسازمانی است و نشت داده میان سازمان‌ها یک شکست امنیتی بحرانی محسوب می‌شود.

### پیامد اجرایی

- `organization_id` برای داده سازمانی الزامی است.
- زمینه سازمان باید در هر تراکنش معتبر اعمال شود.
- نقش عادی برنامه نباید `BYPASSRLS` داشته باشد.
- Pool اتصال نباید زمینه سازمان درخواست قبلی را نشت دهد.
- روابط مرکب باید اتصال منابع دو سازمان را رد کنند.
- Worker، Scheduler، Queue، Search و Report نیز باید زمینه سازمان را حفظ کنند.
- آزمون منفی بین‌سازمانی برای هر دامنه الزامی است.

### شرایط تغییر

کاهش سطح جداسازی مجاز نیست. تغییر مدل چندسازمانی نیازمند تصمیم امنیتی و Migration کامل است.

## DEC-011 — مدل دسترسی با پیش‌فرض عدم دسترسی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: هویت، سازمان، تیم، پرونده، فایل، گزارش و عملیات مدیریتی

### تصمیم

مدل دسترسی ترکیبی است و می‌تواند از نقش، ویژگی، رابطه، تیم، مسئولیت جاری، وضعیت منبع و عدم دسترسی صریح استفاده کند.

قواعد قطعی:

- پیش‌فرض، عدم دسترسی است.
- عدم دسترسی صریح بر اجازه اولویت دارد.
- نقش به‌تنهایی همیشه کافی نیست.
- مشاهده و تغییر می‌توانند مجوزهای مستقل داشته باشند.
- مدیر فنی یا مدیر سازمان به‌صورت ضمنی حق عبور از جداسازی ندارد.

### دلیل

RBAC ساده برای سناریوهای مسئولیت، تیم، وضعیت و منابع سازمانی کافی نیست.

### پیامد اجرایی

- تصمیم دسترسی باید ورودی و نتیجه قابل آزمون داشته باشد.
- عملیات حساس باید Audit شوند.
- پیام عدم دسترسی در رابط عمومی باید فارسی باشد.
- دانستن شناسه منبع اجازه دسترسی ایجاد نمی‌کند.
- Mention، Follow یا دریافت اعلان، مجوز مشاهده جدید ایجاد نمی‌کند.

### شرایط تغییر

تغییر مدل دسترسی نیازمند بازبینی امنیتی، مهاجرت Policy و آزمون منفی کامل است.

## DEC-012 — ثبت اتمی نتیجه و ادامه معتبر

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: پرونده، اقدام، انتظار، مانع، توقف و تصمیم

### تصمیم

ثبت نتیجه یک اقدام یا پایان یک وضعیت پیگیری نباید پرونده را بدون ادامه معتبر رها کند.

نتیجه و ادامه معتبر باید در یک مرز اتمی ثبت شوند.

ادامه معتبر می‌تواند یکی از موارد زیر باشد:

- اقدام بعدی؛
- انتظار داخلی؛
- انتظار خارجی؛
- مانع؛
- توقف موقت؛
- درخواست تصمیم؛
- حل پرونده؛
- لغو کنترل‌شده.

### دلیل

هدف اصلی محصول جلوگیری از فراموشی و بن‌بست جریان است.

### پیامد اجرایی

- پرونده فعال نباید بدون کار جاری روشن باقی بماند.
- کار جاری اصلی باید یکتا باشد.
- انتقال وضعیت ناسازگار رد می‌شود.
- رد مسئولیت نباید پرونده را بدون مسئول یا ادامه رها کند.
- تغییر نتیجه و ادامه باید Timeline و Audit تولید کند.

### شرایط تغییر

حذف این ناوردا با تعریف محصول ناسازگار است و فقط با تغییر رسمی مشخصات مادر محصول ممکن است.

## DEC-013 — تفکیک یادآور، رخداد موعد و تحویل اعلان

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: Scheduler، Worker، Queue، اعلان داخل سامانه، مرورگر، ایمیل و پیامک

### تصمیم

سه مفهوم زیر مستقل هستند:

1. قصد یادآوری؛
2. رخداد موعد؛
3. تحویل اعلان.

### دلیل

تغییر زمان یادآور، Retry تحویل، چندکاناله‌بودن و بازیابی شکست‌ها بدون این تفکیک قابل اعتماد نیست.

### پیامد اجرایی

- Reminder دارای Revision است.
- رخداد متعلق به Revision قدیمی نباید تحویل شود.
- ایجاد رخداد موعد باید هم‌کنش‌پذیر باشد.
- هر تلاش تحویل باید وضعیت و شماره تلاش مستقل داشته باشد.
- تحویل فنی با مشاهده کاربر یکسان نیست.
- Retry، Snooze، Escalation و Dead Letter باید تاریخچه خود را حفظ کنند.
- کانال‌های هدف نسخه نخست:
  - اعلان داخل سامانه؛
  - اعلان مرورگر؛
  - ایمیل؛
  - پیامک.
- Provider نهایی هر کانال هنوز تصمیم جداگانه می‌خواهد.

### شرایط تغییر

ترکیب این مفاهیم در یک جدول یا وضعیت واحد بدون ADR جدید مجاز نیست.

## DEC-014 — تفکیک رویداد، خط زمانی و ممیزی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام دامنه‌ها و عملیات حساس

### تصمیم

مفاهیم زیر مستقل‌اند:

- Domain Event؛
- Integration Event؛
- Timeline Entry؛
- Audit Event.

### دلیل

مخاطب، سطح جزئیات، سیاست نگهداری و کاربرد این چهار مفهوم متفاوت است.

### پیامد اجرایی

- Timeline برای فهم کاربر طراحی می‌شود.
- Audit برای ردیابی امنیتی و فنی است.
- Integration Event باید نسخه و شناسه داشته باشد.
- تحویل رویداد بیرونی باید با Outbox امن شود.
- دریافت تکراری باید با Inbox یا سازوکار معادل مهار شود.
- Audit افزایشی است و حذف عادی ندارد.
- داده محرمانه غیرضروری نباید در Audit یا Log ثبت شود.

### شرایط تغییر

استفاده یک رکورد واحد برای همه این اهداف نیازمند تصمیم معماری و اثبات حفظ همه الزامات است.

## DEC-015 — فایل خصوصی و دسترسی مشتق‌شده از منبع والد

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: بارگذاری، دریافت، پیش‌نمایش، اسکن، نگهداری و حذف فایل

### تصمیم

- فایل‌ها به‌صورت پیش‌فرض خصوصی هستند.
- دسترسی فایل از مجوز منبع والد مشتق می‌شود.
- فایل تا پایان اعتبارسنجی و کنترل امنیتی آماده استفاده نیست.
- URL انتقال باید کوتاه‌عمر و محدود به عمل مشخص باشد.

### دلیل

وجود فایل در ذخیره‌ساز نباید امکان دسترسی مستقیم یا عبور از مجوز سازمانی ایجاد کند.

### پیامد اجرایی

- فراداده فایل در PostgreSQL ثبت می‌شود.
- فایل و منبع والد باید متعلق به یک سازمان باشند.
- نوع اعلام‌شده و محتوای واقعی بررسی می‌شوند.
- محدودیت اندازه و نوع اعمال می‌شود.
- فایل مشکوک قرنطینه می‌شود.
- اسکن بدافزار پیش از آماده‌بودن انجام می‌شود.
- حذف بر اساس مجوز، وابستگی و سیاست نگهداری انجام می‌شود.

### شرایط تغییر

عمومی‌شدن Bucket یا مستقل‌شدن مجوز فایل از منبع والد نیازمند تصمیم امنیتی جداگانه است.

## DEC-016 — گزارش تاریخی بر پایه Snapshot تغییرناپذیر

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: گزارش‌های ارسالی، خروجی PDF، XLSX و CSV

### تصمیم

گزارش تاریخی باید از Snapshot ثابت و تغییرناپذیر تولید شود.

Snapshot باید شامل یا قابل ردیابی به موارد زیر باشد:

- سازمان؛
- نوع گزارش؛
- پارامترها؛
- زمان قطع داده؛
- محتوا؛
- اثرانگشت یکپارچگی؛
- ایجادکننده؛
- نسخه قرارداد.

### دلیل

تغییر داده زنده نباید گزارش تاریخی ثبت‌شده را تغییر دهد.

### پیامد اجرایی

- خروجی تاریخی از Snapshot تولید می‌شود.
- تولید دوباره از همان Snapshot باید سازگار باشد.
- مدل خواندنی منبع حقیقت نیست.
- دریافت خروجی باید مجوز را دوباره بررسی کند.
- حذف فایل خروجی نباید Snapshot یا تاریخچه گزارش را حذف کند.
- تاریخ عمومی خروجی شمسی و زمان فنی UTC است.

### شرایط تغییر

گزارش تاریخی مبتنی بر پرس‌وجوی دوباره داده زنده پذیرفته نیست، مگر برای گزارش زنده که صریحاً از Snapshot جدا تعریف شده باشد.

## DEC-017 — معیار بستن مرحله

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام مراحل نقشه راه

### تصمیم

هیچ زیرمرحله یا مرحله مادری صرفاً با وجود کد، Build یا آزمون واحد بسته نمی‌شود.

بسته‌شدن نیازمند موارد مرتبط زیر است:

- خروجی مورد انتظار؛
- قالب‌بندی؛
- Lint؛
- Typecheck؛
- آزمون مثبت؛
- آزمون منفی؛
- آزمون یکپارچه؛
- آزمون جداسازی سازمانی در صورت ارتباط؛
- Build یا اجرای واقعی؛
- ممیزی UTF-8 و رابط عمومی؛
- به‌روزرسانی اسناد؛
- ثبت Journal؛
- بررسی Diff؛
- Commit؛
- Tag تعریف‌شده؛
- فضای کاری پاک.

### دلیل

ادعای تکمیل باید قابل بازتولید و قابل ممیزی باشد.

### پیامد اجرایی

- نتیجه هر دروازه باید از خروجی واقعی ثبت شود.
- آزمون Skipشده یا حذف‌شده بدون دلیل پذیرفته نیست.
- کاهش تعداد آزمون باید بررسی شود.
- Tag فقط بعد از Commit و پذیرش ایجاد می‌شود.
- مرحله بعد پیش از بسته‌شدن رسمی مرحله جاری آغاز نمی‌شود.

### شرایط تغییر

معیار هر مرحله می‌تواند سخت‌گیرانه‌تر شود، اما کاهش آن نیازمند تصمیم و تحلیل ریسک است.

## DEC-018 — رمزگذاری و نگهداری فایل‌های متنی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: کد، تنظیمات و مستندات متنی

### تصمیم

فایل‌های متنی پروژه باید:

- UTF-8 باشند؛
- BOM نداشته باشند؛
- خط پایانی استاندارد داشته باشند؛
- فاصله انتهای خط نداشته باشند؛
- متن فارسی سالم داشته باشند.

در PowerShell، خواندن و نوشتن متن فارسی با `[IO.File]` و Encoding صریح انجام می‌شود.

### دلیل

Windows PowerShell می‌تواند با Encoding پیش‌فرض موجب خرابی متن فارسی یا ایجاد BOM ناخواسته شود.

### پیامد اجرایی

- استفاده از `[IO.File]::ReadAllText` و `[IO.File]::WriteAllText` ترجیح دارد.
- UTF-8 باید به‌صورت سخت‌گیرانه اعتبارسنجی شود.
- Prettier و `git diff --check` پیش از ثبت اجرا می‌شوند.
- متن خراب نباید با حدس‌زدن بازسازی شود.

### شرایط تغییر

تغییر Encoding مخزن نیازمند تصمیم و تبدیل کنترل‌شده تمام فایل‌ها است.

## DEC-019 — هم‌زمانی تغییر کد و مستندات

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: تمام تغییرات محصول، معماری، پشته، قرارداد و عملیات

### تصمیم

تغییر واقعیت فنی یا محصول باید در همان مرحله در اسناد مالک آن نیز ثبت شود.

حداقل نگاشت:

- وضعیت جاری: `PROJECT-STATUS.md`
- مسیر آینده: `ROADMAP.md`
- اجرای واقعی: `IMPLEMENTATION-JOURNAL.md`
- تصمیم: `DECISIONS.md`
- مشخصات محصول: `PRODUCT-SPECIFICATION.md`
- واژگان و ناوردای‌ها: `DOMAIN-GLOSSARY.md`
- ریسک و بدهی: `RISKS-ASSUMPTIONS-DEBT.md`
- ردیابی نیاز تا آزمون: `TRACEABILITY-MATRIX.md`

### دلیل

به‌روزرسانی دیرهنگام مستندات موجب اختلاف میان کد و مرجع ادامه پروژه می‌شود.

### پیامد اجرایی

- Commit قابلیت باید مستندات مرتبط را نیز شامل شود.
- مستندات نباید موفقیتی را زودتر از کد اعلام کنند.
- تغییر تاریخی نباید با بازنویسی سکوت‌آمیز حذف شود.
- اختلاف کد و مستندات باید پیش از مرحله بعد اصلاح شود.

### شرایط تغییر

هیچ تغییر محصولی یا معماری از این قاعده مستثنا نیست.

## DEC-020 — ترتیب فعلی ادامه پروژه

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: انتقال از `P1.3` به `P1.4`

### تصمیم

مرحله `P1.3.6` تا تکمیل پذیرش فنی و مستنداتی، Commit، Tag و فضای کاری پاک باز می‌ماند.

آغاز رسمی `P1.4.1` فقط پس از بسته‌شدن `P1.3.6` مجاز است.

### دلیل

پذیرش فنی اولیه موفق بوده است، اما مجموعه مستندات و ثبت رسمی مرحله هنوز تکمیل نشده‌اند.

### پیامد اجرایی

پیش از `P1.4.1` باید:

1. اسناد موجود فایل‌به‌فایل بازبینی شوند؛
2. اسناد ضروری باقی‌مانده ایجاد شوند؛
3. ممیزی متقابل انجام شود؛
4. `pnpm check` دوباره اجرا شود؛
5. Build تازه چهار برنامه اجرا شود؛
6. Smoke هماهنگ اجرا شود؛
7. UTF-8 نهایی بررسی شود؛
8. Journal و وضعیت به‌روزرسانی شوند؛
9. Commit پذیرش ایجاد شود؛
10. Tagهای پذیرش ایجاد شوند؛
11. فضای کاری پاک تأیید شود.

### شرایط تغییر

تغییر این ترتیب نیازمند تصمیم ثبت‌شده و دلیل روشن برای پذیرش ریسک است.

## DEC-021 — نام فنی محصول و نام تجاری فارسی

- وضعیت: `پذیرفته‌شده`
- دامنه اثر: کد، مخزن، مستندات و رابط عمومی

### تصمیم

نام فنی و مرجع فعلی پروژه `OrgaWork` است.

نام تجاری فارسی نهایی هنوز تصویب نشده است و نباید نام جدیدی بدون تصمیم رسمی در رابط یا مستندات به‌عنوان برند قطعی معرفی شود.

### دلیل

استفاده زودهنگام از نام فارسی تأییدنشده موجب پراکندگی برند و هزینه تغییر در کد، فایل، رابط و مستندات می‌شود.

### پیامد اجرایی

- نام فنی بسته‌ها و مخزن تا تصمیم جدید حفظ می‌شود.
- ترجمه توصیفی محصول مجاز است، اما نباید به‌عنوان نام تجاری قطعی معرفی شود.
- انتخاب نام تجاری فارسی باید به‌عنوان تصمیم باز ثبت و سپس با تصمیم مستقل بسته شود.

### شرایط تغییر

با تصویب نام تجاری فارسی، تصمیم جدید باید دامنه تغییر برند، Migration نام‌ها و سازگاری فنی را مشخص کند.

# تصمیم‌های جایگزین‌شده یا لغوشده

در زمان این بازبینی هیچ تصمیم اجرایی ثبت‌شده‌ای به‌صورت رسمی جایگزین یا لغو نشده است.

# موضوعاتی که هنوز تصمیم قطعی نیستند

موارد زیر در این سند تصمیم پذیرفته‌شده محسوب نمی‌شوند:

- نام تجاری فارسی نهایی؛
- مدل قیمت‌گذاری؛
- لایسنس؛
- دوره آزمایشی؛
- نسخه رایگان؛
- Provider نهایی ایمیل؛
- Provider نهایی پیامک؛
- Provider نهایی اعلان مرورگر؛
- دامنه عملیاتی؛
- زیرساخت میزبانی نهایی؛
- موتور جست‌وجوی مستقل؛
- برنامه بومی موبایل؛
- پرتال بیرونی؛
- اتصال به پیام‌رسان‌ها؛
- قابلیت‌های هوش مصنوعی؛
- ابزار نهایی Migration و دسترسی PostgreSQL؛
- مقادیر نهایی محدودیت فایل؛
- سیاست نهایی نگهداری؛
- SLO و SLA نهایی.

این موضوعات باید در `RISKS-ASSUMPTIONS-DEBT.md` به‌عنوان تصمیم باز ثبت شوند و پیش از مرحله نیازمند آن‌ها بسته شوند.

# قاعده جایگزینی تصمیم

برای جایگزینی یک تصمیم:

1. مشکل تصمیم فعلی ثبت شود.
2. گزینه‌های جایگزین مقایسه شوند.
3. اثر بر محصول، داده، امنیت و عملیات بررسی شود.
4. تصمیم جدید شناسه مستقل دریافت کند.
5. برنامه مهاجرت نوشته شود.
6. آزمون بازگشتی تعریف شود.
7. تصمیم قبلی `جایگزین‌شده` علامت بخورد.
8. تاریخچه قبلی حذف نشود.
9. اسناد وابسته هم‌زمان اصلاح شوند.
10. Commit و Tag مرحله مرتبط ثبت شوند.
