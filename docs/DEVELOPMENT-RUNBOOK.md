# راهنمای توسعه OrgaWork

## 1. شناسنامه سند

- شناسه سند: `DEVELOPMENT-RUNBOOK`
- نسخه: `0.2.0`
- وضعیت: راهنمای عملی محیط توسعه
- تاریخ ایجاد: `2026-07-28`
- تاریخ بازبینی فنی: `2026-08-04`
- شاخه مرجع: `main`
- مسیر مرجع مخزن در محیط فعلی:

```text
C:\Users\Abtin Akbari\Desktop\OrgaWork
```

- مسیر مرجع pnpm در محیط فعلی:

```text
C:\Users\Abtin Akbari\AppData\Local\OrgaWork\pnpm\pnpm.cmd
```

- اسناد مرتبط:
  - `README.md`
  - `PROJECT-STATUS.md`
  - `ROADMAP.md`
  - `IMPLEMENTATION-JOURNAL.md`
  - `DECISIONS.md`
  - `TEST-AND-ACCEPTANCE.md`
  - `CONTINUATION-PROTOCOL.md`

این سند روش نصب، کنترل، ساخت، اجرا، توقف و عیب‌یابی پروژه را ثبت می‌کند.

این سند:

- وضعیت جاری پروژه را جایگزین نمی‌کند؛
- نقشه راه را جایگزین نمی‌کند؛
- نتیجه تاریخی آزمون‌ها را جایگزین Journal نمی‌کند؛
- Secret واقعی نگهداری نمی‌کند؛
- وجود ابزار یا سرویس آینده را بدون ممیزی فرض نمی‌کند.

# بخش اول — اصول اجرای امن

## 2. اصل یک فرمان در هر نوبت

در گردش کار فعلی توسعه:

1. در هر نوبت فقط یک فرمان PowerShell اجرا می‌شود.
2. هدف فرمان پیش از اجرا روشن می‌شود.
3. خروجی کامل فرمان بررسی می‌شود.
4. موفقیت پیش از مشاهده خروجی فرض نمی‌شود.
5. سپس فقط گام بعدی ارائه می‌شود.

این قاعده برای جلوگیری از موارد زیر است:

- اجرا در مسیر نادرست؛
- تغییر فایل نامرتبط؛
- بازنویسی ناخواسته؛
- Commit زودهنگام؛
- ادامه بر اساس فرض؛
- ازبین‌رفتن متن فارسی؛
- پنهان‌شدن خطای میانی.

## 3. قالب فرمان‌های تغییردهنده

فرمان‌های تغییردهنده باید تا حد امکان در قالب زیر اجرا شوند:

```powershell
& {
    $ErrorActionPreference = 'Stop'

    # بررسی مسیر، شاخه و پیش‌شرط‌ها
    # اجرای یک تغییر محدود
    # اعتبارسنجی نتیجه
}
```

قواعد:

- `$ErrorActionPreference` باید `Stop` باشد.
- مسیر مخزن باید صریح تعیین شود.
- شاخه باید پیش از تغییر بررسی شود.
- وضعیت Git باید پیش از تغییر بررسی شود.
- فایل ناشناخته نباید حذف یا بازنشانی شود.
- Commit و Tag نباید پیش از پذیرش ساخته شوند.
- خروجی خطا نباید پنهان شود.

## 4. قواعد فایل‌های متنی

فایل‌های متنی پروژه باید:

- UTF-8 باشند؛
- BOM نداشته باشند؛
- خط پایانی استاندارد داشته باشند؛
- فاصله انتهای خط نداشته باشند؛
- متن فارسی سالم داشته باشند.

در PowerShell برای فایل‌های فارسی استفاده از این روش ترجیح دارد:

```powershell
[IO.File]::ReadAllText($path)
[IO.File]::WriteAllText($path, $text, [Text.UTF8Encoding]::new($false))
```

از خواندن و نوشتن ضمنی متن فارسی با Encoding نامشخص باید پرهیز شود.

## 5. ممنوعیت‌های عملیاتی

بدون بررسی و دلیل روشن نباید این عملیات انجام شوند:

- `git reset --hard`
- `git clean -fd`
- حذف پوشه ناشناخته
- بازنویسی کل فایل با محتوای حدسی
- تغییر دستی `pnpm-lock.yaml`
- تغییر مستقیم خروجی تولیدشده
- حذف آزمون ناموفق برای سبزشدن Build
- ساخت Tag پیش از Commit پذیرفته‌شده
- ثبت Secret در `.env.example`
- اجرای Migration روی پایگاه داده ناشناخته
- پاک‌سازی Volume بدون تأیید دامنه داده

# بخش دوم — محیط مرجع فعلی

## 6. سیستم عامل و پوسته

محیط توسعه فعلی:

- Windows
- PowerShell
- مخزن محلی در Desktop کاربر

فرمان‌ها باید با PowerShell سازگار باشند.

نباید از ابزارهای مخصوص Bash مانند موارد زیر بدون معادل PowerShell استفاده شود:

- `tail`
- `grep`
- `sed`
- `awk`
- `rm -rf`

نمونه معادل‌ها:

| نیاز          | PowerShell                   |
| ------------- | ---------------------------- |
| آخرین ردیف‌ها | `Select-Object -Last`        |
| جست‌وجوی متن  | `Select-String`              |
| حذف کنترل‌شده | `Remove-Item` با بررسی پیشین |
| فهرست فایل    | `Get-ChildItem`              |
| مسیر واقعی    | `Resolve-Path`               |

## 7. ابزارهای شناخته‌شده

نسخه‌های ثبت‌شده در وضعیت فعلی:

| ابزار        |      نسخه |
| ------------ | --------: |
| Node.js      | `24.18.0` |
| Corepack     |  `0.35.0` |
| pnpm         | `11.17.0` |
| TypeScript   |   `6.0.3` |
| Turborepo    |  `2.10.7` |
| Vitest       |  `4.1.10` |
| ESLint       |  `10.8.0` |
| Prettier     |   `3.9.6` |
| tsx          |  `4.23.1` |
| Next.js      | `16.2.10` |
| React        |  `19.2.8` |
| Tailwind CSS |   `4.3.3` |
| Fastify      |  `5.10.0` |

مرجع نهایی نسخه‌های اجرایی:

- `package.json` ریشه؛
- `package.json` هر Workspace؛
- `pnpm-lock.yaml`.

در صورت اختلاف این جدول با مخزن، ابتدا اختلاف بررسی و سپس این سند به‌روزرسانی می‌شود.

## 8. هویت Git مرجع

هویت ثبت‌شده برای محیط فعلی:

```text
Arman Akbari
javad.akbari.s@gmail.com
```

پیش از Commit باید هویت واقعی Git بررسی شود.

## 9. شاخه مرجع

شاخه توسعه مرجع:

```text
main
```

آغاز یا ادامه مرحله روی شاخه دیگر بدون تصمیم صریح مجاز نیست.

## 10. مسیر pnpm

در محیط فعلی pnpm از این مسیر اجرا می‌شود:

```text
C:\Users\Abtin Akbari\AppData\Local\OrgaWork\pnpm\pnpm.cmd
```

در فرمان‌های حساس بهتر است از مسیر کامل استفاده شود تا وابستگی به `PATH` کاهش یابد.

# بخش سوم — ساختار مخزن

## 11. پوشه‌های اصلی

ساختار اصلی Workspace:

```text
OrgaWork
├── apps
├── packages
├── modules
├── infra
├── tools
├── docs
├── package.json
├── pnpm-workspace.yaml
└── pnpm-lock.yaml
```

## 12. برنامه‌های اجرایی

چهار برنامه اجرایی فعلی:

| برنامه    | مسیر             | نقش                  |
| --------- | ---------------- | -------------------- |
| Web       | `apps/web`       | رابط کاربری          |
| API       | `apps/api`       | رابط HTTP            |
| Worker    | `apps/worker`    | پردازش پس‌زمینه      |
| Scheduler | `apps/scheduler` | تشخیص و اجرای موعدها |

این چهار برنامه در وضعیت فعلی Bootstrap اجرایی دارند.

قابلیت‌های تجاری دامنه هنوز از وجود این چهار برنامه نتیجه نمی‌شوند.

## 13. پوشه packages

پوشه `packages` برای بسته‌های مشترک فنی استفاده می‌شود.

قواعد:

- منطق دامنه نامرتبط در یک بسته عمومی جمع نمی‌شود.
- بسته مشترک نباید مرز دامنه را دور بزند.
- وابستگی چرخه‌ای ممنوع است.
- نام و نقش هر بسته باید روشن باشد.
- بسته باید آزمون و کنترل کیفیت متناسب داشته باشد.

## 14. پوشه modules

پوشه `modules` برای ماژول‌های دامنه هدف‌گذاری شده است.

قواعد:

- ماژول دامنه نباید به برنامه اجرایی وابسته باشد.
- قواعد کسب‌وکار در برنامه Web یا API پراکنده نمی‌شوند.
- مرز Aggregate و Contract پیش از پیاده‌سازی دامنه تثبیت می‌شود.
- هر ماژول باید جداسازی سازمانی و مجوز را رعایت کند.

## 15. پوشه infra

پوشه `infra` برای زیرساخت محلی و عملیاتی است.

موارد آینده:

- Docker Compose؛
- PostgreSQL؛
- Redis؛
- MinIO؛
- تنظیمات محیط؛
- Health Check؛
- Volume؛
- منابع استقرار.

وجود پوشه `infra` به معنی وجود یا آماده‌بودن زیرساخت نیست.

## 16. پوشه tools

پوشه `tools` برای Scriptها و کنترل‌های Workspace استفاده می‌شود.

فایل‌های شناخته‌شده مرتبط با اجرای هماهنگ:

```text
tools/scripts/start-applications.ts
tools/checks/coordinated-applications-smoke.ts
```

نام و وجود واقعی فایل‌ها پیش از تغییر باید در مخزن بررسی شوند.

## 17. پوشه docs

پوشه `docs` منبع دائمی مستندات پروژه است.

هیچ مرحله‌ای نباید فقط در گفت‌وگو یا حافظه باقی بماند.

# بخش چهارم — فرمان‌های ریشه

## 18. نصب وابستگی‌ها

فرمان مرجع نصب باید از فایل قفل استفاده کند.

فرمان دقیق باید از Scriptها و سیاست جاری مخزن خوانده شود.

اصل پذیرفته‌شده:

```text
pnpm install --frozen-lockfile
```

در محیط فعلی باید مسیر کامل pnpm در نظر گرفته شود.

پیش از نصب:

- مسیر مخزن بررسی شود؛
- فایل قفل وجود داشته باشد؛
- وضعیت Git ثبت شود؛
- Scriptهای ساخت وابستگی بررسی شوند؛
- دسترسی شبکه و Registry در صورت نیاز بررسی شود.

## 19. کنترل TypeScript

Script ریشه:

```text
typecheck
```

هدف:

- بررسی TypeScript تمام Workspaceهای مرتبط؛
- جلوگیری از خطای نوع پیش از Build؛
- تشخیص Drift میان بسته‌ها.

## 20. کنترل کیفیت کد

Scriptهای ریشه:

```text
lint
lint:fix
```

قاعده:

- `lint` برای پذیرش استفاده می‌شود.
- `lint:fix` فقط پس از بررسی Diff استفاده می‌شود.
- اصلاح خودکار نباید تغییر معنایی پنهان ایجاد کند.

## 21. قالب‌بندی

Scriptهای ریشه:

```text
format
format:check
```

قاعده:

- `format:check` در دروازه پذیرش اجرا می‌شود.
- `format` فقط در دامنه فایل‌های مورد انتظار اجرا می‌شود.
- فایل تولیدشده خودکار نباید بدون سیاست مشخص قالب‌بندی شود.

## 22. آزمون

Scriptهای ریشه:

```text
test
test:watch
test:coverage
```

کاربرد:

- `test`: اجرای آزمون‌ها؛
- `test:watch`: توسعه محلی؛
- `test:coverage`: پوشش آزمون.

تعداد آزمون ثابت نیست و باید از خروجی همان اجرا خوانده شود.

آخرین شواهد ثبت‌شده پیش از نهایی‌سازی مستندات:

- ۹ فایل آزمون؛
- ۴۴ آزمون موفق.

این اعداد باید هنگام پذیرش دوباره بررسی شوند.

## 23. کنترل یکپارچه

Script ریشه:

```text
check
```

در وضعیت فعلی این Script کنترل‌های اصلی Workspace را ترکیب می‌کند.

نتیجه دقیق باید از `package.json` ریشه خوانده شود.

## 24. ساخت برنامه‌ها

Script ریشه:

```text
build:apps
```

هدف:

- ساخت Web؛
- ساخت API؛
- ساخت Worker؛
- ساخت Scheduler.

برای پذیرش مرحله، Build باید تازه و بدون اتکا به Cache قبلی بررسی شود.

## 25. توسعه هماهنگ

Script ریشه:

```text
dev:apps
```

هدف:

- اجرای هم‌زمان برنامه‌ها برای توسعه.

این فرمان فرایند ماندگار ایجاد می‌کند.

پیش از اجرا:

- پورت‌ها بررسی شوند؛
- فرایند قدیمی باقی نمانده باشد؛
- متغیرهای محیطی معتبر باشند.

## 26. اجرای خروجی‌های ساخته‌شده

Script ریشه:

```text
start:apps
```

هدف:

- اجرای خروجی Buildشده چهار برنامه.

این فرمان جایگزین آزمون پذیرش نیست و باید همراه بررسی واقعی برنامه‌ها استفاده شود.

## 27. آزمون دود هماهنگ

Script ریشه:

```text
smoke:apps
```

آزمون دود شناخته‌شده باید موارد زیر را بررسی کند:

- آزادبودن پورت‌های لازم؛
- اجرای Web؛
- فارسی و RTL بودن صفحه پایه؛
- اجرای API؛
- پاسخ `/health`؛
- اجرای یک‌مرحله‌ای Worker؛
- اجرای یک‌مرحله‌ای Scheduler؛
- کد خروج صحیح؛
- توقف هماهنگ؛
- آزادشدن پورت‌ها.

# بخش پنجم — برنامه Web

## 28. مسیر و پورت

- مسیر: `apps/web`
- پورت پیش‌فرض: `3000`

## 29. فناوری

- Next.js `16.2.10`
- React `19.2.8`
- Tailwind CSS `4.3.3`

## 30. الزامات قطعی Web

- `lang="fa"`
- `dir="rtl"`
- متن عمومی فارسی
- عدم نمایش انگلیسی خام
- عدم نمایش خطای فنی خام
- تاریخ عمومی هجری شمسی
- زمان فنی UTC
- ارقام عمومی تا حد امکان فارسی
- Focus قابل مشاهده
- استفاده با صفحه‌کلید
- حالت‌های Loading، Empty، Error و Access Denied

## 31. وضعیت فعلی Web

پیاده‌سازی‌شده و آزموده‌شده:

- Bootstrap Next.js؛
- صفحه پایه فارسی؛
- RTL؛
- عنوان فارسی؛
- ابزار پایه نمایش تاریخ شمسی؛
- آزمون متن پایه؛
- Build؛
- اجرای واقعی مرورگر.

هنوز پیاده‌سازی‌شده تلقی نمی‌شود:

- هویت؛
- سازمان؛
- پرونده؛
- اقدام؛
- داشبورد واقعی؛
- فایل؛
- گزارش؛
- جست‌وجو.

## 32. کنترل تاریخ در Web

قاعده:

- ورودی و نمایش عمومی شمسی است.
- ذخیره و انتقال UTC و ISO است.
- منطقه زمانی با شناسه IANA مدیریت می‌شود.
- Offset ثابت جایگزین منطقه زمانی نیست.
- تاریخ نامعتبر باید رد شود.
- زمان محلی کنترل‌نشده منبع حقیقت نیست.

# بخش ششم — برنامه API

## 33. مسیر و پورت

- مسیر: `apps/api`
- پورت پیش‌فرض: `3001`
- Host پیش‌فرض: `127.0.0.1`

## 34. فناوری

- Fastify `5.10.0`

## 35. مسیر سلامت فعلی

```http
GET /health
```

پاسخ پایه شامل:

- شناسه سرویس؛
- وضعیت؛
- Timestamp استاندارد UTC و ISO.

پاسخ Health باید Cache نشود.

## 36. وضعیت فعلی API

پیاده‌سازی‌شده و آزموده‌شده:

- سازنده مستقل برنامه؛
- مسیر `/health`؛
- پاسخ Not Found؛
- اعتبارسنجی Host و Port؛
- توقف امن؛
- Build؛
- اجرای واقعی.

هنوز پیاده‌سازی‌شده تلقی نمی‌شود:

- OpenAPI کامل؛
- نشست؛
- سازمان جاری؛
- PostgreSQL؛
- RLS؛
- Endpointهای دامنه؛
- مجوز تجاری؛
- Idempotency عمومی؛
- Audit دامنه.

## 37. توقف امن API

API باید در دریافت Signalهای پایان:

- پذیرش درخواست جدید را متوقف کند؛
- منابع را ببندد؛
- پورت را آزاد کند؛
- خروج کنترل‌شده داشته باشد.

Signalهای شناخته‌شده:

- `SIGINT`
- `SIGTERM`

# بخش هفتم — Worker

## 38. مسیر

```text
apps/worker
```

## 39. متغیرهای محیطی

متغیرهای شناخته‌شده:

| متغیر                     | معنا              |
| ------------------------- | ----------------- |
| `WORKER_NAME`             | نام Worker        |
| `WORKER_POLL_INTERVAL_MS` | فاصله Poll        |
| `WORKER_RUN_ONCE`         | اجرای یک‌مرحله‌ای |

مقادیر پیش‌فرض شناخته‌شده:

| مقدار             |           پیش‌فرض |
| ----------------- | ----------------: |
| نام               | `orgawork-worker` |
| فاصله Poll        | `5000` میلی‌ثانیه |
| اجرای یک‌مرحله‌ای |           `false` |

محدوده فاصله معتبر ثبت‌شده:

```text
100 .. 300000
```

## 40. وضعیت فعلی Worker

پیاده‌سازی‌شده و آزموده‌شده:

- پیکربندی محیطی؛
- اعتبارسنجی؛
- چرخه قابل آزمون؛
- اجرای یک‌مرحله‌ای؛
- توقف امن؛
- رخداد ساختاریافته پایه؛
- زمان UTC و ISO؛
- Build؛
- اجرای واقعی با کد خروج صفر.

هنوز پیاده‌سازی‌شده تلقی نمی‌شود:

- Redis Queue؛
- Outbox Consumer؛
- Inbox؛
- Lease واقعی؛
- Retry Policy دامنه؛
- Dead Letter واقعی؛
- عملیات دامنه؛
- Provider اعلان.

## 41. اجرای یک‌مرحله‌ای Worker

حالت `WORKER_RUN_ONCE` برای آزمون واقعی و Smoke استفاده می‌شود.

انتظار پایه:

- یک چرخه اجرا شود؛
- رخداد تکمیل ثبت شود؛
- رخداد توقف ثبت شود؛
- کد خروج صفر باشد.

تعداد رخداد باید از خروجی واقعی همان اجرا بررسی شود.

# بخش هشتم — Scheduler

## 42. مسیر

```text
apps/scheduler
```

## 43. متغیرهای محیطی

متغیرهای شناخته‌شده:

| متغیر                   | معنا              |
| ----------------------- | ----------------- |
| `SCHEDULER_NAME`        | نام Scheduler     |
| `SCHEDULER_INTERVAL_MS` | فاصله اجرا        |
| `SCHEDULER_RUN_ONCE`    | اجرای یک‌مرحله‌ای |

مقادیر پیش‌فرض شناخته‌شده:

| مقدار             |              پیش‌فرض |
| ----------------- | -------------------: |
| نام               | `orgawork-scheduler` |
| فاصله             |   `60000` میلی‌ثانیه |
| اجرای یک‌مرحله‌ای |              `false` |

محدوده فاصله معتبر:

```text
100 .. 86400000
```

## 44. وضعیت فعلی Scheduler

پیاده‌سازی‌شده و آزموده‌شده:

- پیکربندی محیطی؛
- اعتبارسنجی؛
- تابع محاسبه موعد بعدی؛
- کاهش Drift تجمعی؛
- اجرای یک‌مرحله‌ای؛
- توقف امن؛
- رخداد ساختاریافته پایه؛
- زمان UTC و ISO؛
- Build؛
- اجرای واقعی با کد خروج صفر.

هنوز پیاده‌سازی‌شده تلقی نمی‌شود:

- Reminder Intent؛
- Due Event پایدار؛
- Query پایگاه داده؛
- Queue واقعی؛
- Lock توزیع‌شده؛
- سیاست Catch-up؛
- Calendar Rule دامنه.

## 45. قاعده محاسبه موعد بعدی

موعد بعدی از موعد برنامه‌ریزی‌شده قبلی محاسبه می‌شود، نه صرفاً از زمان پایان اجرای فعلی.

هدف:

- کاهش Drift تجمعی.

سیاست دقیق شکست، عقب‌ماندگی و اجرای جبرانی در مرحله `P5` تثبیت می‌شود.

# بخش نهم — پورت‌ها و فرایندها

## 46. پورت‌های فعلی

| برنامه |   پورت |
| ------ | -----: |
| Web    | `3000` |
| API    | `3001` |

Worker و Scheduler در وضعیت فعلی پورت عمومی ثابت ندارند.

## 47. بررسی پیش از اجرا

پیش از اجرای هماهنگ باید بررسی شود:

- پورت `3000` آزاد است؛
- پورت `3001` آزاد است؛
- فرایند Node قدیمی مرتبط باقی نمانده است؛
- Build لازم وجود دارد یا قرار است ساخته شود؛
- متغیرهای محیطی معتبرند.

## 48. توقف فرایندها

توقف باید:

- ابتدا از مسیر کنترل‌شده انجام شود؛
- Process Tree مربوط را پاک کند؛
- فرایند نامرتبط را متوقف نکند؛
- آزادشدن پورت‌ها را تأیید کند.

نباید همه فرایندهای Node سیستم بدون تشخیص مالک متوقف شوند.

## 49. نشانه توقف ناقص

موارد زیر می‌توانند نشانه توقف ناقص باشند:

- پورت همچنان Listening است؛
- فرمان جدید خطای Address in Use می‌دهد؛
- Process فرزند باقی مانده است؛
- Smoke بعدی پیش از اجرا شکست می‌خورد؛
- فایل Lock موقت باقی مانده است.

# بخش دهم — متغیرهای محیطی

## 50. اصول عمومی

- Secret واقعی در Git ثبت نمی‌شود.
- `.env.example` فقط Placeholder امن دارد.
- مقدار پیش‌فرض فقط برای توسعه محلی و غیرمحرمانه مجاز است.
- متغیر نامعتبر باید در Startup رد شود.
- خطای تنظیمات باید روشن و بدون افشای Secret باشد.
- تنظیمات هر برنامه باید Schema اعتبارسنجی داشته باشد.

## 51. متغیرهای فعلی شناخته‌شده

### API

متغیرهای دقیق باید از کد و فایل نمونه خوانده شوند.

موارد مفهومی شناخته‌شده:

- Host
- Port

### Worker

- `WORKER_NAME`
- `WORKER_POLL_INTERVAL_MS`
- `WORKER_RUN_ONCE`

### Scheduler

- `SCHEDULER_NAME`
- `SCHEDULER_INTERVAL_MS`
- `SCHEDULER_RUN_ONCE`

### Web

متغیر عمومی جدید نباید بدون بررسی امنیتی در Browser منتشر شود.

## 52. متغیرهای زیرساخت محلی

متغیرهای PostgreSQL، Redis، MinIO، Bucket، Endpoint و Credential محلی در `P1.4` تعریف و از طریق `packages/configuration` اعتبارسنجی شده‌اند.

نام‌های دقیق باید از `.env.example` و کد پیکربندی خوانده شوند. مقدار Secret فقط در `.env.local` نگهداری می‌شود و ثبت آن در Git، گزارش آزمون یا خروجی عمومی ممنوع است.

# بخش یازدهم — Git

## 53. بررسی پیش از تغییر

پیش از هر تغییر:

- مسیر مخزن بررسی شود؛
- شاخه بررسی شود؛
- `git status` خوانده شود؛
- فایل‌های تغییرکرده شناخته شوند؛
- Commit آخر بررسی شود؛
- Tagهای مرتبط بررسی شوند.

## 54. فایل ناشناخته

اگر فایل تغییرکرده یا Untracked ناشناخته وجود دارد:

1. تغییر متوقف شود.
2. فایل خوانده شود.
3. منشأ آن مشخص شود.
4. تصمیم گرفته شود:
   - حفظ؛
   - ثبت؛
   - بازگردانی؛
   - حذف کنترل‌شده.
5. هیچ انتخابی بدون شواهد انجام نشود.

## 55. Commit

Commit فقط پس از:

- کنترل کیفیت؛
- آزمون؛
- Build؛
- اجرای واقعی مرتبط؛
- بررسی Diff؛
- به‌روزرسانی اسناد؛
- پذیرش مرحله

ایجاد می‌شود.

پیام Commit باید:

- شناسه مرحله را داشته باشد؛
- خروجی اصلی را توضیح دهد؛
- ادعای بیش از واقعیت نکند.

## 56. Tag

Tag:

- پس از Commit ساخته می‌شود؛
- باید به Commit درست اشاره کند؛
- باید با نام مرحله سازگار باشد؛
- پیش از ثبت، نبود Tag متعارض بررسی می‌شود؛
- پس از ثبت، قابل Resolve بودن آن بررسی می‌شود.

## 57. Commitها و Tagهای شناخته‌شده

| مرحله    | Commit                                     | Tag                                                         |
| -------- | ------------------------------------------ | ----------------------------------------------------------- |
| `P1.1`   | `ffe3980`                                  | Tag مستقل شناخته‌شده ندارد                                  |
| `P1.2`   | `00d9119`                                  | `stage-p1.2-typescript-linting-formatting-test-foundations` |
| `P1.3.1` | `e3302824ae2f185207fddf95f60c542c92a345be` | `stage-p1.3.1-persian-rtl-web-application-bootstrap`        |
| `P1.3.2` | `ac905ae820b98eef16a8b251e5304dbf096398d6` | `stage-p1.3.2-fastify-api-application-bootstrap`            |
| `P1.3.3` | `f3fba3a1bde97e11d44e7d21e9702badd4514b85` | `stage-p1.3.3-background-worker-application-bootstrap`      |
| `P1.3.4` | `448a7fa910c299b27f8201041e61595ba7a07e3c` | `stage-p1.3.4-scheduler-application-bootstrap`              |
| `P1.3.5` | `25e195c055e6446d16bfd0106e51510fa0457c2b` | `stage-p1.3.5-coordinated-application-execution`            |

پذیرش `P1.3.6` با شواهد رسمی زیر ثبت شده است:

- Commit پذیرش: `089a7066e31cd413cfce3a5246ee0038cc2e5e73`
- Tag پذیرش: `stage-p1.3.6-executable-applications-acceptance`
- Tag تکمیل مرحله مادر: `stage-p1.3-executable-applications-bootstrap-complete`

هر دو Tag سبک و اشاره‌کننده به Commit پذیرش هستند.

# بخش دوازدهم — دروازه کنترل کیفیت

## 58. کنترل پایه

پیش از پذیرش هر تغییر مرتبط باید کنترل‌های متناسب اجرا شوند:

- Prettier؛
- ESLint؛
- TypeScript؛
- Vitest؛
- Build؛
- Smoke یا اجرای واقعی؛
- UTF-8؛
- `git diff --check`.

## 59. آزمون مثبت

آزمون می‌کند رفتار معتبر کار می‌کند.

نمونه:

- Port معتبر پذیرفته شود؛
- `/health` پاسخ موفق بدهد؛
- تاریخ معتبر قالب‌بندی شود؛
- Worker یک چرخه اجرا کند.

## 60. آزمون منفی

آزمون می‌کند ورودی یا انتقال نامعتبر رد می‌شود.

نمونه:

- Port نامعتبر؛
- Boolean نامعتبر؛
- فاصله زمانی خارج از محدوده؛
- تاریخ نامعتبر؛
- مسیر ناشناخته؛
- سازمان یا مجوز نامعتبر در مراحل آینده.

## 61. آزمون یکپارچه

آزمون یکپارچه فقط Unit Test چند فایل نیست.

باید مرز واقعی مرتبط را پوشش دهد، مانند:

- API و PostgreSQL؛
- Worker و Queue؛
- Scheduler و Due Event؛
- Web و API؛
- فایل و ذخیره‌ساز؛
- Report و Snapshot.

## 62. آزمون چندسازمانی

از زمان ورود داده سازمانی، هر قابلیت باید حداقل این موارد را پوشش دهد:

- عضو مجاز همان سازمان؛
- عضو بدون مجوز همان سازمان؛
- کاربر سازمان دیگر؛
- منبع سازمان دیگر؛
- شناسه مستقیم منبع دیگر؛
- Search مختلط؛
- Report مختلط؛
- File سازمان دیگر؛
- Queue با زمینه سازمان نادرست.

## 63. Build تازه

برای پذیرش مرحله:

- نتیجه Cacheشده نباید تنها شاهد باشد.
- Build باید روی وضعیت نهایی فایل‌ها اجرا شود.
- شکست یک برنامه نباید با موفقیت برنامه دیگر پنهان شود.
- خروجی Build باید بررسی شود.

## 64. ممیزی UTF-8

ممیزی باید دست‌کم موارد زیر را بررسی کند:

- UTF-8 معتبر؛
- نبود BOM؛
- نبود `U+FFFD`؛
- نبود الگوهای Mojibake؛
- نبود فاصله انتهای خط؛
- وجود خط پایانی استاندارد.

# بخش سیزدهم — عیب‌یابی

## 65. pnpm پیدا نمی‌شود

بررسی:

- مسیر کامل pnpm؛
- وجود فایل `pnpm.cmd`؛
- مجوز اجرا؛
- نسخه؛
- مسیر مخزن.

در محیط فعلی مسیر مرجع:

```text
C:\Users\Abtin Akbari\AppData\Local\OrgaWork\pnpm\pnpm.cmd
```

## 66. فایل package.json پیدا نمی‌شود

علت رایج:

- PowerShell در پوشه نادرست است.

بررسی:

- مسیر فعلی؛
- وجود `package.json`؛
- نام صحیح پوشه؛
- Extract ناقص یا Nested Folder.

هیچ فرمان نصب یا Build پیش از تأیید مسیر اجرا نشود.

## 67. Port در حال استفاده است

بررسی:

- Process مالک پورت؛
- اجرای قبلی Web یا API؛
- Process فرزند باقی‌مانده؛
- ابزار دیگر سیستم.

فقط Process مرتبط با OrgaWork متوقف شود.

## 68. آزمون‌ها کمتر شده‌اند

کاهش تعداد آزمون می‌تواند ناشی از:

- حذف فایل؛
- تغییر Pattern؛
- Skip؛
- Workspace اجرا‌نشده؛
- خطای Discovery

باشد.

تعداد کمتر بدون بررسی نباید پذیرفته شود.

## 69. Build از Cache می‌آید

برای پذیرش باید Build تازه اثبات شود.

روش دقیق بر اساس Script و Turborepo مخزن تعیین می‌شود.

نباید Cache به‌صورت گسترده و بدون دلیل حذف شود.

## 70. متن فارسی خراب شده است

اقدام:

1. تغییر متوقف شود.
2. Diff بررسی شود.
3. Encoding فایل بررسی شود.
4. نسخه سالم از Git یا فایل منبع بازیابی شود.
5. متن از روی ظاهر خراب حدس زده نشود.
6. نوشتن مجدد با UTF-8 بدون BOM انجام شود.
7. Prettier و ممیزی متن اجرا شوند.

## 71. Git وضعیت پاک ندارد

ابتدا تغییرها طبقه‌بندی شوند:

- تغییر مورد انتظار؛
- فایل تولیدشده؛
- فایل موقت؛
- تغییر کاربر؛
- تغییر ناشناخته.

هیچ `reset` یا `clean` عمومی اجرا نشود.

## 72. Smoke در Cleanup شکست می‌خورد

بررسی:

- Signal به Parent رسیده است؛
- Child Process باقی مانده است؛
- Port آزاد شده است؛
- Timeout کافی است؛
- Windows Process Tree درست مدیریت شده است؛
- خطای برنامه پیش از Cleanup رخ داده است.

## 73. API Health موفق است اما سامانه آماده نیست

Health پایه فعلی فقط Bootstrap API را نشان می‌دهد.

در آینده باید تفکیک شود:

- Liveness؛
- Readiness؛
- Health.

موفقیت `/health` فعلی به معنی آماده‌بودن PostgreSQL، Redis، MinIO یا قابلیت‌های تجاری نیست.

## 74. Worker یا Scheduler خروج صفر دارد اما کاری انجام نشده

در حالت یک‌مرحله‌ای فعلی هدف آزمون Bootstrap است.

پس از اتصال واقعی باید بررسی شود:

- پیام یا موعد واقعی وجود داشته است؛
- سازمان معتبر بوده است؛
- Idempotency رعایت شده است؛
- نتیجه پایدار ثبت شده است؛
- Retry یا شکست پنهان نشده است.

# بخش چهاردهم — زیرساخت محلی پذیرفته‌شده

## 75. وضعیت فعلی

زیرساخت محلی در `P1.4` و پذیرش نهایی `P1` اجرا و آزموده شده است:

- Docker Desktop، Docker Engine و Docker Compose فعال‌اند؛
- PostgreSQL `16.14`، Redis `7.4.10` و MinIO با نسخه و Digest ثابت اجرا می‌شوند؛
- شبکه داخلی و Volumeهای نام‌دار پایدار وجود دارند؛
- Bucket خصوصی `orgawork-files` ایجاد و کنترل شده است؛
- Health، اتصال واقعی چهار برنامه و ماندگاری داده پس از Restart پذیرفته شده‌اند.

## 76. فرمان‌های رسمی

```powershell
pnpm infra:start
pnpm infra:report
pnpm infra:stop
pnpm infra:persistence
pnpm infra:cleanup
```

پیش از هر فرمان `pnpm` در اجرای خودکار باید `PATH` پردازش از ترکیب سطح Machine و User بازسازی شود.

## 77. قواعد ایمنی

- `infra:cleanup` فقط با هدف صریح حذف داده محلی اجرا شود؛
- Volume ناشناخته یا متعلق به پروژه دیگر حذف نشود؛
- فایل Compose آرشیوی جایگزین نسخه مخزن نشود؛
- نسخه یا Digest سرویس‌ها بدون تصمیم ثبت‌شده تغییر نکند؛
- Credential واقعی در Git، سند یا گزارش ثبت نشود؛
- Tenant Context فقط داخل Transaction و با `SET LOCAL` اعمال شود.

## 78. سرویس‌های پذیرفته‌شده

- PostgreSQL `16.14`
- Redis `7.4.10`
- MinIO `RELEASE.2025-09-07T16-13-09Z`
- شبکه داخلی اختصاصی
- Volumeهای پایدار PostgreSQL، Redis و MinIO
- Health Check و Readiness
- Bucket خصوصی `orgawork-files`

## 79. معیار پذیرش و بازآزمایی

- `pnpm infra:start` با سه سرویس سالم پایان یابد؛
- Initializer یک‌باره Bucket با کد خروج صفر پایان یابد؛
- Bucket خصوصی باقی بماند؛
- اتصال واقعی برنامه‌ها به سه سرویس موفق باشد؛
- داده پس از توقف و آغاز مجدد حفظ شود؛
- Migrationها Rerun و Fingerprint امن داشته باشند؛
- RLS و جداسازی سازمانی روی PostgreSQL واقعی آزموده شوند؛
- Smoke چهار برنامه موفق باشد؛
- هیچ Secret واقعی در Git یا خروجی عمومی ظاهر نشود.

# بخش پانزدهم — امنیت عملیاتی توسعه

## 80. Secret

نباید در موارد زیر ثبت شود:

- Git؛
- مستندات؛
- Screenshot؛
- Log؛
- پیام خطا؛
- فایل نمونه؛
- خروجی آزمون.

## 81. فایل‌های محیطی

قاعده:

- فایل واقعی محیط Local باقی می‌ماند؛
- فایل نمونه فقط نام متغیر و Placeholder امن دارد؛
- مقدار محرمانه در Commit نمی‌رود؛
- تغییر فایل محیطی در `git status` بررسی می‌شود.

## 82. داده آزمایشی

داده آزمایشی نباید:

- اطلاعات واقعی حساس داشته باشد؛
- داده سازمان واقعی را کپی کند؛
- Secret واقعی داشته باشد؛
- به شماره یا ایمیل واقعی پیام بفرستد؛
- در Screenshot عمومی هویت واقعی نشان دهد.

## 83. Log

Log فنی نباید شامل:

- گذرواژه؛
- Token؛
- Secret؛
- محتوای کامل فایل؛
- Payload حساس کامل؛
- Connection String عملیاتی.

## 84. وابستگی‌ها

افزودن وابستگی باید بررسی کند:

- ضرورت؛
- مجوز؛
- نسخه؛
- نگهداری؛
- امنیت؛
- اندازه؛
- اثر Build؛
- سازگاری Node و TypeScript.

# بخش شانزدهم — به‌روزرسانی این Runbook

## 85. رویدادهای الزام‌آور

این سند باید هنگام وقوع موارد زیر به‌روزرسانی شود:

- تغییر مسیر مخزن؛
- تغییر شاخه مرجع؛
- تغییر نسخه ابزار اصلی؛
- تغییر مسیر pnpm؛
- افزودن یا حذف Script؛
- تغییر Port؛
- تغییر متغیر محیطی؛
- افزودن سرویس زیرساخت؛
- تغییر روش Build یا Smoke؛
- تغییر روش توقف؛
- کشف خطای عیب‌یابی جدید؛
- تغییر گردش کار PowerShell.

## 86. اطلاعاتی که نباید در این سند ثبت شوند

- Secret واقعی؛
- Token؛
- گذرواژه؛
- Connection String عملیاتی؛
- محتوای داده واقعی؛
- نتیجه تاریخی همه اجراها؛
- تصمیم باز به‌عنوان واقعیت؛
- نام Contract حدسی.

## 87. معیار پذیرش این سند

این Runbook معتبر است اگر:

- مسیر و ابزارهای فعلی را درست ثبت کند؛
- Scriptهای شناخته‌شده را پوشش دهد؛
- نقش چهار برنامه را توضیح دهد؛
- Portها را ثبت کند؛
- متغیرهای Worker و Scheduler را ثبت کند؛
- روش Git امن را توضیح دهد؛
- قواعد UTF-8 را ثبت کند؛
- عیب‌یابی پایه را پوشش دهد؛
- زیرساخت آینده را به‌عنوان موجود اعلام نکند؛
- با وضعیت و نقشه راه سازگار باشد.

## 88. چک‌لیست آغاز جلسه توسعه

1. مسیر مخزن درست است.
2. شاخه `main` است.
3. `git status` خوانده شده است.
4. Commit آخر شناخته شده است.
5. مرحله جاری از `PROJECT-STATUS.md` خوانده شده است.
6. ردیف بعدی از `ROADMAP.md` خوانده شده است.
7. تصمیم‌های مرتبط خوانده شده‌اند.
8. فایل‌های تغییرکرده شناخته شده‌اند.
9. ابزار لازم ممیزی شده است.
10. فقط یک فرمان بعدی تعیین شده است.

## 89. چک‌لیست پایان جلسه توسعه

1. خروجی آخرین فرمان ثبت شده است.
2. تغییرهای انجام‌شده شناخته شده‌اند.
3. خطای حل‌نشده پنهان نشده است.
4. آزمون مرتبط اجرا شده است.
5. وضعیت Git خوانده شده است.
6. Journal در صورت نیاز به‌روزرسانی شده است.
7. گام بعدی دقیق ثبت شده است.
8. Commit و Tag زودهنگام ایجاد نشده‌اند.
9. فرایند ماندگار ناخواسته باقی نمانده است.
10. Portهای آزمایشی آزادند.

## 90. اصل نهایی

این Runbook باید امکان دهد توسعه‌دهنده‌ای که فقط به مخزن دسترسی دارد:

- محیط را شناسایی کند؛
- ابزارها را بررسی کند؛
- پروژه را نصب کند؛
- کنترل کیفیت را اجرا کند؛
- چهار برنامه را بسازد؛
- اجرای هماهنگ را بررسی کند؛
- فرایندها را امن متوقف کند؛
- خطاهای رایج را عیب‌یابی کند؛
- بدون حدس به مرحله بعد برسد.

هرجا این سند جزئیات کافی ندارد، ابتدا وضعیت واقعی مخزن بررسی و سپس سند به‌روزرسانی می‌شود.

<!-- ORGAWORK:UNIFIED-VERIFICATION-WORKFLOW:START -->

# پیوست جاری — گردش یکپارچه Verification و اختتام Stage

از این پس فرمان‌های پراکنده آزمون، Build و پذیرش برای کار روزمره مستقیماً انتخاب نمی‌شوند. منبع اجرای استاندارد `tools/verification` است.

## پروفایل‌های توسعه

```text
pnpm verify:fast
pnpm verify:stage -- --stage <STAGE_ID>
pnpm verify:full
pnpm verify:infra
```

- `verify:fast`: بازخورد سریع روی فایل‌های تغییرکرده، تست‌های مرتبط و کنترل معماری/امنیت مرتبط.
- `verify:stage`: فقط Gateهای ثبت‌شده همان Stage در `tools/verification/stages.ts`.
- `verify:full`: ممیزی کامل مخزن برای نقاط پذیرش مهم.
- `verify:infra`: Migration، معماری و امنیت زیرساختی.
- `verify:ci`: پروفایل داخلی GitHub Actions است و کاربر معمولاً آن را مستقیم اجرا نمی‌کند.

## قاعده اختتام Stage

ترتیب استاندارد اختتام:

1. تغییرات فنی Stage کامل می‌شوند.
2. Technical Commit ایجاد می‌شود و Worktree پاک است.
3. `pnpm verify:stage -- --stage <STAGE_ID>` روی همان Commit اجرا و PASS می‌شود.
4. `pnpm stage:close:prepare -- --stage <STAGE_ID> --evidence <EVIDENCE_ID>` مستندات الزامی و Acceptance Report را تولید و Stage می‌کند.
5. Diff مستندات بررسی می‌شود.
6. `pnpm stage:close:publish -- --stage <STAGE_ID> --evidence <EVIDENCE_ID>` Closure Commit و Tag پذیرش را ایجاد و به‌صورت Atomic به `origin/main` Push می‌کند.
7. Remote main، Tag و Worktree پاک دوباره کنترل می‌شوند.

Closure برای Stageای که Stage-specific evidence gate ثبت‌شده ندارد مجاز نیست.

گزارش Verification محلی داخل `.git/orgawork/verification` نگهداری می‌شود تا Worktree کثیف نشود. در CI گزارش در `artifacts/verification` قرار می‌گیرد.

<!-- ORGAWORK:UNIFIED-VERIFICATION-WORKFLOW:END -->

<!-- ORGAWORK:RUNBOOK:VERIFICATION-SYSTEM -->

## Verification system

تعریف کامل روش انتخاب Gate، Acceptance، Closure، Publication و Clean-runner در `docs/VERIFICATION-SYSTEM.md` نگهداری می‌شود. Runbook نباید تعریف موازی و متناقضی از این چرخه ایجاد کند.
