# دفتر اجرای فنی OrgaWork

## 1. شناسنامه سند

- شناسه سند: `IMPLEMENTATION-JOURNAL`
- نسخه بازبینی‌شده: `0.2.0`
- وضعیت: دفتر تاریخی اجرای واقعی پروژه
- تاریخ بازبینی فنی: `2026-08-02`
- شاخه مرجع: `main`
- مرجع وضعیت جاری: `PROJECT-STATUS.md`
- مرجع ترتیب آینده: `ROADMAP.md`
- مرجع تصمیم‌های الزام‌آور: `DECISIONS.md` و `APPROVED-BASELINES.md`

این سند تاریخچه اقداماتی را ثبت می‌کند که واقعاً در مخزن یا محیط توسعه انجام شده‌اند.
این فایل نباید جایگزین وضعیت جاری، نقشه راه یا تصمیم معماری شود.

## 2. قواعد ثبت در Journal

برای هر مرحله باید تا حد امکان موارد زیر ثبت شوند:

1. هدف مرحله؛
2. وضعیت پیش از اجرا؛
3. خروجی‌های ایجادشده یا تغییریافته؛
4. تصمیم‌ها و محدودیت‌های مهم؛
5. خطاهای مشاهده‌شده؛
6. اصلاحات انجام‌شده؛
7. آزمون‌های اجراشده؛
8. نتیجه Build و اجرای واقعی؛
9. دامنه فایل‌های تغییرکرده؛
10. Commit و Tag؛
11. نتیجه نهایی و مرحله بعد.

قواعد قطعی:

- فقط نتیجه مشاهده‌شده ثبت می‌شود.
- وجود کد بدون آزمون، «آزموده‌شده» محسوب نمی‌شود.
- وجود طراحی یا Contract، «پیاده‌سازی‌شده» محسوب نمی‌شود.
- موفقیت Build به‌تنهایی به معنی پذیرش مرحله نیست.
- بسته آرشیوی یا گفت‌وگوی قبلی، شاهد اجرای مخزن فعلی نیست.
- خطاها نباید از تاریخچه حذف شوند.
- اگر مرحله هنوز Commit یا Tag ندارد، باید صریحاً ثبت شود.
- زمان فنی در خروجی برنامه‌ها باید UTC و ISO باشد.
- رابط عمومی باید فارسی و راست‌به‌چپ باشد.
- فایل‌های متنی فارسی باید UTF-8 بدون BOM باشند.

# P0 — بنیاد محصول و تصمیم‌های پیش از توسعه

## 3. هدف

تثبیت مسئله محصول، محدوده نسخه نخست، معماری، مدل دامنه، داده، قراردادها، تجربه کاربری و دروازه آمادگی توسعه پیش از آغاز برنامه‌نویسی.

## 4. خروجی‌های پذیرفته‌شده

اسناد زیر با نسخه شناخته‌شده `0.1.0` پذیرفته شدند:

- `MASTER-SPEC-001`
- `ADR-001`
- `ADR-002`
- `ADR-003`
- `ADR-004`
- `ADR-005`
- `ADR-006`
- `ADR-007`
- `ADR-008`
- `ADR-009`
- `ADR-010`
- `DDM-001`
- `DBS-001`
- `ACS-001`
- `UXS-001`
- `DRS-001`

## 5. تصمیم‌های کلیدی ثبت‌شده

- محصول یک سامانه سازمانی چندکاربره و چندسازمانی است.
- معماری کلان یکپارچه ماژولار است.
- چهار برنامه اجرایی مستقل هدف‌گذاری شدند:
  - Web؛
  - API؛
  - Worker؛
  - Scheduler.
- PostgreSQL منبع نهایی وضعیت پایدار تجاری است.
- Redis منبع نهایی حقیقت تجاری نیست.
- فایل‌ها در ذخیره‌ساز سازگار با S3 نگهداری می‌شوند.
- فراداده فایل‌ها در PostgreSQL ثبت می‌شود.
- جداسازی داده سازمان‌ها باید سخت‌گیرانه باشد.
- سیاست پیش‌فرض دسترسی، عدم دسترسی است.
- رابط عمومی باید فارسی و RTL باشد.
- تاریخ عمومی شمسی و زمان فنی UTC است.
- نتیجه اقدام بدون ادامه معتبر نباید ثبت شود.
- Timeline، Audit، Domain Event و Integration Event مفاهیم جدا هستند.
- گزارش تاریخی باید بر Snapshot تغییرناپذیر استوار باشد.
- Event Sourcing کامل و Microservices در محدوده نسخه نخست تصویب نشدند.

## 6. نتیجه P0

- وضعیت: بسته‌شده
- اجازه آغاز `P1` صادر شد.
- این مرحله به معنی پیاده‌سازی قابلیت‌های تجاری نبود.
- خلاصه دائمی تصمیم‌ها در `APPROVED-BASELINES.md` نگهداری می‌شود.

# P1.1 — ایجاد ساختار فضای کاری

## 7. هدف

ایجاد مخزن، Workspace، پوشه‌های اصلی و مرزهای اولیه برنامه‌ها، بسته‌ها، ماژول‌ها، زیرساخت و مستندات.

## 8. انجام‌شده

- پوشه پروژه ایجاد شد.
- مخزن Git ایجاد شد.
- شاخه اصلی `main` تثبیت شد.
- Node.js نسل 24 بررسی شد.
- pnpm نسخه `11.17.0` به‌صورت کاربری نصب شد.
- فایل `package.json` ریشه ایجاد شد.
- فایل `pnpm-workspace.yaml` ایجاد شد.
- پوشه‌های اصلی ایجاد شدند:
  - `apps`
  - `packages`
  - `modules`
  - `infra`
  - `tools`
  - `docs`
- برنامه‌های اجرایی پایه ایجاد شدند:
  - `apps/web`
  - `apps/api`
  - `apps/worker`
  - `apps/scheduler`
- بسته‌های مشترک پایه ایجاد شدند.
- ماژول‌های دامنه پایه ایجاد شدند.
- پوشه‌های زیرساخت و ابزارهای توسعه ایجاد شدند.
- فایل‌های کنترل پروژه ایجاد شدند.
- فایل‌های پایه مخزن و تنظیم Workspace ایجاد شدند.
- ۴۴ فایل نگه‌دارنده برای ثبت پوشه‌های خالی ایجاد شد.
- ساختار پوشه‌ها بررسی شد.
- اعتبار فایل‌های JSON بررسی شد.
- هویت Git و شاخه اصلی بررسی شدند.

## 9. وضعیت آزمون و محدودیت مرحله

در این مرحله هنوز:

- وابستگی‌های کامل توسعه نصب نشده بودند؛
- Build برنامه‌ها انجام نشده بود؛
- آزمون زمان اجرا انجام نشده بود.

بنابراین نتیجه مرحله فقط ایجاد و اعتبارسنجی ساختار Workspace بود.

## 10. ثبت Git

- Commit: `ffe3980`
- پیام Commit:

```text
Stage P1.1: bootstrap pnpm monorepo and workspace structure
```

- Tag مستقل برای `P1.1` در وضعیت شناخته‌شده ثبت نشده است.

## 11. نتیجه P1.1

- وضعیت: بسته‌شده
- ساختار چندبخشی Workspace ایجاد شد.
- برنامه‌ها، بسته‌های مشترک و ماژول‌های دامنه تفکیک شدند.
- مرحله بعد: `P1.2 — ابزارهای پایه توسعه`

# P1.2 — ابزارهای پایه توسعه

## 12. هدف

راه‌اندازی TypeScript، Lint، قالب‌بندی، آزمون، پوشش و کنترل یکپارچه Workspace.

## 13. انجام‌شده

- ابزارهای پایه نصب شدند:
  - TypeScript
  - ESLint
  - Prettier
  - Vitest
  - Turborepo
  - tsx
- نسخه وابستگی‌های پایه به‌صورت دقیق تثبیت شد.
- اجرای Script ساخت `esbuild` به‌صورت صریح مجاز شد.
- بسته مشترک تنظیم TypeScript ایجاد شد.
- بسته مشترک تنظیم ESLint ایجاد شد.
- قالب‌بندی خودکار فعال شد.
- بررسی قالب‌بندی بدون تغییر فعال شد.
- بررسی سخت‌گیرانه TypeScript فعال شد.
- آزمون پایه فعال شد.
- پوشش آزمون فعال شد.
- فرمان کنترل یکپارچه ایجاد شد.
- فایل قفل تثبیت و بررسی شد.

## 14. نسخه‌های شناخته‌شده در پایان مرحله

- Node.js: `24.18.0`
- Corepack: `0.35.0`
- pnpm: `11.17.0`
- TypeScript: `6.0.3`
- Turborepo: `2.10.7`
- Vitest: `4.1.10`
- ESLint: `10.8.0`
- Prettier: `3.9.6`
- tsx: `4.23.1`

## 15. فرمان‌های ریشه ایجادشده

- `typecheck`
- `lint`
- `lint:fix`
- `format`
- `format:check`
- `test`
- `test:watch`
- `test:coverage`
- `check`

## 16. نتیجه آزمون

- قالب‌بندی: موفق
- بررسی کیفیت کد: موفق
- بررسی TypeScript: موفق
- آزمون‌ها: `1` آزمون موفق
- پوشش آزمون نمونه پایه: `100%`
- بررسی فایل قفل: موفق

## 17. ثبت Git

- Commit: `00d9119`
- Tag:

```text
stage-p1.2-typescript-linting-formatting-test-foundations
```

## 18. نتیجه P1.2

- وضعیت: بسته‌شده
- Workspace برای راه‌اندازی برنامه‌های اجرایی آماده شد.
- مرحله بعد: `P1.3.1 — راه‌اندازی برنامه رابط کاربری`

# P1.3.1 — راه‌اندازی برنامه رابط کاربری

## 19. هدف

ایجاد برنامه Web مستقل، قابل ساخت و قابل اجرا با رابط پایه فارسی، راست‌به‌چپ و تاریخ شمسی.

## 20. انجام‌شده

- برنامه Web با Next.js، React و Tailwind راه‌اندازی شد.
- نسخه وابستگی‌های برنامه Web تثبیت شد.
- Script ساخت وابستگی پردازش تصویر به‌صورت صریح مجاز شد.
- تنظیم TypeScript سخت‌گیرانه فعال شد.
- ESLint برنامه Web فعال شد.
- پوسته اولیه فارسی و RTL ایجاد شد.
- ویژگی‌های سند تنظیم شدند:
  - `lang="fa"`
  - `dir="rtl"`
- تمام متن‌های قابل مشاهده صفحه آغازین فارسی شدند.
- فرهنگ مرکزی متن‌های رابط کاربری ایجاد شد.
- آزمون جلوگیری از حروف انگلیسی در متن‌های قابل مشاهده ایجاد شد.
- ابزار نمایش تاریخ هجری شمسی با ارقام فارسی ایجاد شد.
- تبدیل تاریخ بر اساس UTC و منطقه زمانی IANA طراحی شد.
- تاریخ نامعتبر آزموده شد.
- منطقه زمانی خالی آزموده شد.
- فایل تولیدشده خودکار Next.js از قالب‌بندی مستثنا شد.
- متن به‌هم‌ریخته تنظیمات ریشه تعمیر شد.
- رمزگذاری فایل‌های متنی ممیزی شد.
- اجرای واقعی صفحه روی پورت محلی انجام شد.
- پاسخ موفق، زبان فارسی، RTL و عنوان صفحه تأیید شدند.
- فرایند آزمایشی متوقف شد.
- پورت محلی پس از آزمون آزاد شد.

## 21. نسخه‌های اصلی برنامه Web

- Next.js: `16.2.10`
- React: `19.2.8`
- Tailwind CSS: `4.3.3`

## 22. نتیجه آزمون

- بررسی قالب‌بندی: موفق
- ESLint: موفق
- TypeScript: موفق
- آزمون‌های Workspace: `7` آزمون موفق
- آزمون‌های اختصاصی Web: `6` آزمون موفق
- Build برنامه Web: موفق
- تولید ایستای صفحه اصلی: موفق
- اجرای واقعی مرورگر: موفق
- ممیزی رمزگذاری: موفق
- آزادشدن پورت: موفق

## 23. ثبت Git

- Commit:

```text
e3302824ae2f185207fddf95f60c542c92a345be
```

- Tag:

```text
stage-p1.3.1-persian-rtl-web-application-bootstrap
```

## 24. نتیجه P1.3.1

- وضعیت: بسته‌شده
- برنامه Web پایه قابل ساخت و اجرا است.
- قابلیت‌های تجاری دامنه هنوز پیاده‌سازی نشده‌اند.
- مرحله بعد: `P1.3.2 — راه‌اندازی برنامه رابط برنامه‌نویسی`

# P1.3.2 — راه‌اندازی برنامه رابط برنامه‌نویسی

## 25. هدف

ایجاد API مستقل مبتنی بر Fastify با مسیر سلامت، پیکربندی معتبر و توقف امن.

## 26. انجام‌شده

- بسته مستقل API ایجاد شد.
- Fastify نسخه `5.10.0` تثبیت شد.
- Scriptهای توسعه، ساخت، اجرا، Lint، Typecheck و Test تعریف شدند.
- تنظیم TypeScript سمت سرور ایجاد شد.
- تنظیم مستقل Build ایجاد شد.
- سازنده مستقل برنامه برای اجرا و آزمون ایجاد شد.
- مسیر سلامت ایجاد شد:

```http
GET /health
```

- پاسخ سلامت شامل موارد زیر است:
  - شناسه سرویس؛
  - وضعیت؛
  - زمان UTC و ISO.
- جلوگیری از Cache پاسخ سلامت فعال شد.
- میزبان و پورت از متغیر محیطی پشتیبانی می‌کنند.
- مقدارهای پیش‌فرض:
  - Host: `127.0.0.1`
  - Port: `3001`
- پورت نامعتبر رد می‌شود.
- توقف امن در `SIGINT` و `SIGTERM` پیاده‌سازی شد.
- مسیر سلامت با تزریق داخلی درخواست آزموده شد.
- مسیر ناشناخته آزموده شد.
- خروجی ساخته‌شده روی پورت محلی اجرا شد.
- پاسخ واقعی `/health` بررسی شد.
- زمان استاندارد پاسخ بررسی شد.
- سرور پس از آزمون متوقف شد.
- پورت پس از آزمون آزاد شد.

## 27. نتیجه آزمون

- ESLint اختصاصی API: موفق
- TypeScript اختصاصی API: موفق
- آزمون اختصاصی API: `8` آزمون موفق
- Build API: موفق
- آزمون‌های Workspace: `15` آزمون موفق
- اجرای واقعی `/health`: موفق
- اعتبار زمان UTC و ISO: موفق
- آزادشدن پورت: موفق

## 28. ثبت Git

- Commit:

```text
ac905ae820b98eef16a8b251e5304dbf096398d6
```

- Tag:

```text
stage-p1.3.2-fastify-api-application-bootstrap
```

## 29. نتیجه P1.3.2

- وضعیت: بسته‌شده
- API پایه مستقل و قابل آزمون است.
- پایگاه داده و هویت هنوز متصل نشده‌اند.
- مرحله بعد: `P1.3.3 — راه‌اندازی پردازشگر پس‌زمینه`

# P1.3.3 — راه‌اندازی پردازشگر پس‌زمینه

## 30. هدف

ایجاد Worker مستقل، قابل آزمون، قابل توقف و آماده اتصال آینده به Queue و عملیات دامنه.

## 31. انجام‌شده

- بسته مستقل Worker ایجاد شد.
- Scriptهای توسعه، ساخت، اجرا، Lint، Typecheck و Test تعریف شدند.
- تنظیم TypeScript سخت‌گیرانه ایجاد شد.
- تنظیم مستقل Build ایجاد شد.
- پیکربندی از محیط اجرا پشتیبانی می‌کند:
  - `WORKER_NAME`
  - `WORKER_POLL_INTERVAL_MS`
  - `WORKER_RUN_ONCE`
- مقدارهای پیش‌فرض:
  - نام: `orgawork-worker`
  - فاصله Poll: `5000` میلی‌ثانیه
  - اجرای یک‌مرحله‌ای: `false`
- محدوده فاصله معتبر:
  - حداقل: `100`
  - حداکثر: `300000` میلی‌ثانیه
- مقدار فاصله نامعتبر رد می‌شود.
- مقدار Boolean نامعتبر رد می‌شود.
- چرخه Worker مستقل و قابل تزریق ایجاد شد.
- توقف پیش از آغاز چرخه آزموده شد.
- توقف میان چرخه‌ها آزموده شد.
- رخدادهای ساختاریافته برای تکمیل، توقف و شکست ایجاد شدند.
- زمان آغاز و پایان هر چرخه با UTC و ISO ثبت می‌شود.
- اجرای واقعی یک‌مرحله‌ای انجام شد.
- اجرای واقعی دقیقاً دو رخداد تولید کرد:
  - تکمیل چرخه؛
  - توقف.
- کد خروج واقعی `0` بود.
- علت توقف به‌درستی «اجرای یک‌مرحله‌ای» ثبت شد.

## 32. نتیجه آزمون

- ESLint اختصاصی Worker: موفق
- TypeScript اختصاصی Worker: موفق
- آزمون اختصاصی Worker: `12` آزمون موفق
- Build Worker: موفق
- آزمون‌های Workspace: `27` آزمون موفق
- اجرای واقعی: موفق
- کد خروج: `0`
- تعداد رخداد اجرای واقعی: `2`
- چرخه تکمیل‌شده: `1`
- توقف امن: موفق

## 33. ثبت Git

- Commit:

```text
f3fba3a1bde97e11d44e7d21e9702badd4514b85
```

- Tag:

```text
stage-p1.3.3-background-worker-application-bootstrap
```

## 34. نتیجه P1.3.3

- وضعیت: بسته‌شده
- Worker پایه قابل ساخت، اجرا و آزمون است.
- Queue واقعی هنوز متصل نشده است.
- مرحله بعد: `P1.3.4 — راه‌اندازی زمان‌بند`

# P1.3.4 — راه‌اندازی زمان‌بند

## 35. هدف

ایجاد Scheduler مستقل با محاسبه پایدار موعد بعدی، اجرای یک‌مرحله‌ای، توقف امن و گزارش زمان استاندارد.

## 36. انجام‌شده

- بسته مستقل Scheduler ایجاد شد.
- Scriptهای توسعه، ساخت، اجرا، Lint، Typecheck و Test تعریف شدند.
- تنظیم TypeScript سخت‌گیرانه ایجاد شد.
- تنظیم مستقل Build ایجاد شد.
- پیکربندی محیطی ایجاد شد:
  - `SCHEDULER_NAME`
  - `SCHEDULER_INTERVAL_MS`
  - `SCHEDULER_RUN_ONCE`
- مقدارهای پیش‌فرض:
  - نام: `orgawork-scheduler`
  - فاصله اجرا: `60000` میلی‌ثانیه
  - اجرای یک‌مرحله‌ای: `false`
- محدوده فاصله معتبر:
  - حداقل: `100`
  - حداکثر: `86400000` میلی‌ثانیه
- فاصله نامعتبر رد می‌شود.
- Boolean نامعتبر رد می‌شود.
- تابع مستقل محاسبه موعد بعدی ایجاد شد.
- موعد بعدی بر اساس موعد برنامه‌ریزی‌شده قبلی محاسبه می‌شود.
- هدف این روش کاهش Drift تجمعی است.
- موعد نامعتبر آزموده شد.
- فاصله محاسباتی نامعتبر آزموده شد.
- چرخه Scheduler مستقل و قابل توقف ایجاد شد.
- توقف پیش از نخستین اجرا آزموده شد.
- توقف پیش از اجرای بعدی آزموده شد.
- رخداد ساختاریافته شامل زمان‌های زیر ایجاد شد:
  - `scheduledFor`
  - `startedAt`
  - `completedAt`
  - `nextRunAt`
- تمام زمان‌ها UTC و ISO هستند.
- اجرای واقعی یک‌مرحله‌ای انجام شد.
- دقیقاً دو رخداد تولید شد:
  - اجرای زمان‌بندی‌شده؛
  - توقف.
- اختلاف موعد جاری و بعدی با فاصله آزمایشی `250` میلی‌ثانیه تأیید شد.
- کد خروج واقعی `0` بود.
- علت توقف اجرای یک‌مرحله‌ای تأیید شد.

## 37. نتیجه آزمون

- ESLint اختصاصی Scheduler: موفق
- TypeScript اختصاصی Scheduler: موفق
- آزمون اختصاصی Scheduler: `17` آزمون موفق
- Build Scheduler: موفق
- آزمون‌های Workspace: `44` آزمون موفق
- اجرای واقعی: موفق
- کد خروج: `0`
- تعداد رخداد اجرای واقعی: `2`
- اجرای زمان‌بندی‌شده تکمیل‌شده: `1`
- محاسبه موعد بعدی: موفق
- توقف امن: موفق

## 38. ثبت Git

- Commit:

```text
448a7fa910c299b27f8201041e61595ba7a07e3c
```

- Tag:

```text
stage-p1.3.4-scheduler-application-bootstrap
```

## 39. نتیجه P1.3.4

- وضعیت: بسته‌شده
- Scheduler پایه قابل ساخت، اجرا و آزمون است.
- اتصال به Reminder Intent و رخدادهای موعد پایدار هنوز انجام نشده است.
- مرحله بعد: `P1.3.5 — اجرای هماهنگ برنامه‌ها`

# P1.3.5 — اجرای هماهنگ برنامه‌ها

## 40. هدف

ایجاد فرمان‌های ریشه برای ساخت، توسعه، اجرای خروجی و آزمون دود هماهنگ چهار برنامه.

## 41. انجام‌شده

Scriptهای ریشه زیر ایجاد شدند:

- `build:apps`
- `dev:apps`
- `start:apps`
- `smoke:apps`

فایل‌های هماهنگ‌سازی شناخته‌شده:

- `tools/scripts/start-applications.ts`
- `tools/checks/coordinated-applications-smoke.ts`

رفتار اجرا:

- Web روی پورت `3000` اجرا می‌شود.
- API روی پورت `3001` اجرا می‌شود.
- Worker در اجرای عادی ماندگار آغاز می‌شود.
- Scheduler در اجرای عادی ماندگار آغاز می‌شود.
- توقف هماهنگ Process Tree پیاده‌سازی شد.
- خروج غیرمنتظره هر برنامه آغاز پاک‌سازی را تحریک می‌کند.
- آزمون دود پیش از اجرا آزادبودن پورت‌ها را بررسی می‌کند.
- رابط Web در اجرای واقعی بررسی شد.
- فارسی و RTL بودن رابط بررسی شد.
- عنوان اصلی فارسی بررسی شد.
- مسیر سلامت API و محتوای پاسخ بررسی شدند.
- Worker یک چرخه یک‌مرحله‌ای موفق اجرا کرد.
- Scheduler یک اجرای یک‌مرحله‌ای با فاصله آزمایشی `250` میلی‌ثانیه ثبت کرد.
- Worker و Scheduler با کد خروج `0` پایان یافتند.
- Web و API پس از آزمون متوقف شدند.
- پورت‌های `3000` و `3001` پس از پاک‌سازی آزاد شدند.

## 42. نتیجه آزمون

- Prettier Workspace: موفق
- ESLint Workspace: موفق
- TypeScript Workspace: موفق
- آزمون‌های Workspace: `44` آزمون موفق
- Build هماهنگ: `4` برنامه موفق
- اجرای واقعی Web: موفق
- اجرای واقعی API: موفق
- اجرای واقعی Worker: موفق
- اجرای واقعی Scheduler: موفق
- پاک‌سازی Process Tree: موفق
- آزادشدن پورت‌ها: موفق

## 43. ثبت Git

- Commit:

```text
25e195c055e6446d16bfd0106e51510fa0457c2b
```

- Tag:

```text
stage-p1.3.5-coordinated-application-execution
```

## 44. نتیجه P1.3.5

- وضعیت: بسته‌شده
- چهار برنامه دارای فرمان یکپارچه ساخت، توسعه، اجرا و Smoke هستند.
- مرحله بعد: `P1.3.6 — آزمون و پذیرش برنامه‌های اجرایی`

# P1.3.6 — آزمون و پذیرش برنامه‌های اجرایی

## 45. هدف

ممیزی کامل خروجی‌های `P1.3.1` تا `P1.3.5`، اجرای پذیرش تازه و ثبت رسمی بسته‌شدن مرحله مادر `P1.3`.

## 46. وضعیت پیش از پذیرش

- شاخه: `main`
- آخرین زیرمرحله بسته‌شده: `P1.3.5`
- برنامه‌های موجود:
  - Web
  - API
  - Worker
  - Scheduler
- تعداد آزمون‌های شناخته‌شده: `44`
- وضعیت اسناد در آغاز بازبینی: ناکافی برای ادامه مستقل پروژه
- `ROADMAP.md` در نسخه اولیه فقط تا `P1.3.6` ادامه داشت و مراحل آینده را ثبت نکرده بود.

## 47. پذیرش فنی اجراشده

یک دروازه پذیرش فقط‌خواندنی روی وضعیت ثبت‌شده برنامه‌ها اجرا شد.

نتایج مشاهده‌شده:

- برچسب‌های `P1.3.1` تا `P1.3.5` معتبر بودند.
- برچسب‌ها Ancestor وضعیت جاری بودند.
- فایل قفل معتبر بود.
- `pnpm check` موفق بود.
- Prettier موفق بود.
- ESLint موفق بود.
- TypeScript موفق بود.
- Vitest موفق بود.
- تعداد فایل آزمون: `9`
- تعداد آزمون: `44`
- همه آزمون‌ها موفق بودند.
- Build تازه چهار برنامه موفق بود.
- تعداد Build موفق: `4`
- تعداد نتیجه Cacheشده در اجرای پذیرش: `0`
- Smoke واقعی هماهنگ موفق بود.
- رابط فارسی و RTL تأیید شد.
- API Health تأیید شد.
- Worker تأیید شد.
- Scheduler تأیید شد.
- توقف و پاک‌سازی فرایندها موفق بود.
- پورت‌ها پس از آزمون آزاد بودند.
- ممیزی UTF-8 و خرابی متن برای `66` فایل موفق بود.
- فضای کاری پیش از آغاز توسعه مستندات پاک بود.

## 48. نتیجه فنی پذیرش

از نظر فنی، برنامه‌های اجرایی برای ثبت پذیرش `P1.3.6` آماده تشخیص داده شدند.

با این حال مرحله در همان زمان بسته نشد، زیرا:

- نقشه راه آینده کامل نبود؛
- اسناد محصول و معماری در مخزن برای ادامه مستقل کافی نبودند؛
- پروتکل ادامه پس از تغییر گفت‌وگو ثبت نشده بود؛
- Runbook توسعه کامل نبود؛
- معیارهای آزمون و پذیرش در سند مستقل ثبت نشده بودند؛
- ریسک‌ها، فرض‌ها و بدهی‌ها ثبت متمرکز نداشتند؛
- Traceability Matrix موجود نبود؛
- سیاست تغییر مستندات موجود نبود؛
- Commit و Tag پذیرش هنوز ایجاد نشده بودند.

## 49. گسترش نقشه راه

برای جلوگیری از وابستگی ادامه پروژه به گفت‌وگوهای قبلی، `ROADMAP.md` تا `P12` گسترش یافت.

مراحل افزوده‌شده:

- `P1.4` زیرساخت محلی داده و ذخیره‌سازی
- `P1.5` Migration و جداسازی سازمانی
- `P1.6` قراردادهای مشترک و OpenAPI
- `P1.7` مشاهده‌پذیری و سلامت عملیاتی
- `P1.8` CI و آزمون‌های معماری
- `P1.9` بررسی‌های فنی اجباری
- `P1.10` پذیرش نهایی P1
- `P2` هویت، سازمان و مجوز
- `P3` پرونده، مسئولیت و اقدام
- `P4` وضعیت‌های پیگیری و تصمیم
- `P5` یادآور و اعلان
- `P6` Timeline، Audit و همکاری
- `P7` فایل و پیوست
- `P8` گزارش و داشبورد
- `P9` مخاطب، پروژه و جست‌وجو
- `P10` مقاوم‌سازی، امنیت و کارایی
- `P11` استقرار و آمادگی عملیاتی
- `P12` انتشار نسخه `1.0`

وضعیت این تغییر:

- فایل تغییرکرده: `docs/ROADMAP.md`
- Commit در زمان ثبت این بخش: ایجاد نشده بود
- Tag در زمان ثبت این بخش: ایجاد نشده بود

## 50. ممیزی مجموعه مستندات

در ممیزی اولیه فقط چهار سند قدیمی در مخزن شناسایی شدند:

- `DECISIONS.md`
- `IMPLEMENTATION-JOURNAL.md`
- `PROJECT-STATUS.md`
- `ROADMAP.md`

کمبودهای شناسایی‌شده:

- تعریف جامع محصول؛
- محدوده نسخه نخست؛
- خلاصه خط‌مبناهای مصوب؛
- واژه‌نامه دامنه؛
- Runbook توسعه؛
- پروتکل ادامه پروژه؛
- معیار آزمون و پذیرش؛
- ثبت ریسک، فرض و بدهی؛
- Traceability؛
- سیاست تغییر مستندات؛
- فهرست نقش و ترتیب اعتبار اسناد.

## 51. اسناد مرجع ایجادشده یا در حال بازبینی

در جریان تکمیل مستندات، فایل‌های زیر ایجاد یا گسترش یافتند:

- `docs/README.md`
- `docs/PRODUCT-SPECIFICATION.md`
- `docs/APPROVED-BASELINES.md`
- `docs/DOMAIN-GLOSSARY.md`

وضعیت:

- فایل‌ها ایجاد شده‌اند.
- قالب‌بندی آن‌ها بررسی شده است.
- UTF-8 بدون BOM بررسی شده است.
- هنوز Commit یا Tag ایجاد نشده است.
- بازبینی فایل‌به‌فایل برای جلوگیری از حذف جزئیات ادامه دارد.

اسناد برنامه‌ریزی‌شده‌ای که هنوز باید بر اساس مجموعه نهایی ایجاد یا تأیید شوند:

- `DEVELOPMENT-RUNBOOK.md`
- `CONTINUATION-PROTOCOL.md`
- `TEST-AND-ACCEPTANCE.md`
- `RISKS-ASSUMPTIONS-DEBT.md`
- `TRACEABILITY-MATRIX.md`
- `DOCUMENTATION-CHANGE-POLICY.md`

## 52. بازبینی PROJECT-STATUS.md

نسخه اولیه `PROJECT-STATUS.md` فقط شامل موارد زیر بود:

- نام پروژه؛
- فاز کلان؛
- مرحله جاری؛
- آخرین مرحله؛
- مرحله بعد؛
- چند الزام رابط.

کمبودها:

- شواهد مراحل بسته‌شده؛
- Commitها و Tagها؛
- وضعیت چهار برنامه؛
- نسخه ابزارها؛
- پذیرش فنی `P1.3.6`؛
- موارد پیاده‌سازی‌نشده؛
- معیار بسته‌شدن مرحله؛
- وضعیت مستندات؛
- مرحله بعد از پذیرش.

نسخه بازبینی‌شده جداگانه تهیه شد و باید پس از تأیید جایگزین شود.

## 53. بازبینی ROADMAP.md

نسخه گسترش‌یافته از نظر دامنه مناسب بود، اما موارد زیر اصلاح شدند:

- ساختار عنوان‌های `P1.1` تا `P1.10`
- ثبت تاریخی `P0`
- افزودن تعریف وضعیت Checkbox
- افزودن ترتیب اعتبار
- افزودن تعریف پایان هر زیرمرحله
- افزودن وابستگی کلان مراحل
- افزودن شواهد Commit و Tag
- تبدیل `P1.3.6` از یک ردیف مبهم به دروازه ۱۷ مرحله‌ای
- رفع چند عبارت مبهم در مجوز، اعلان، فایل و گزارش
- افزودن قواعد کنترل تغییر نقشه راه

نسخه بازبینی‌شده جداگانه تهیه شد و باید پس از تأیید جایگزین شود.

## 54. بازبینی DOMAIN-GLOSSARY.md

یک فایل واژه‌نامه موجود با `1547` ردیف شناسایی شد.

ممیزی مفهومی نشان داد:

- تعداد مفاهیم بررسی‌شده: `68`
- مفاهیم دارای پوشش: `64`
- مفاهیم فاقد پوشش اولیه: `4`
- متن فارسی سالم بود.
- UTF-8 معتبر بود.
- BOM نداشت.
- فاصله انتهای خط نداشت.
- خط پایانی استاندارد داشت.

موارد تکمیل‌شده:

- کلید هم‌کنشی امن؛
- رخداد ساختاریافته؛
- ریسک؛
- بدهی فنی؛
- فرض؛
- تصمیم باز؛
- ناوردای‌های گزارش.

پس از تکمیل:

- تعداد ردیف‌ها: `1818`
- تعداد عنوان‌ها: `250`
- UTF-8 بدون BOM: معتبر
- Prettier: موفق
- Commit در زمان ثبت این بخش: ایجاد نشده بود
- Tag در زمان ثبت این بخش: ایجاد نشده بود

## 55. نتیجه اجرای P1.3.6

موارد انجام‌شده:

- [x] ممیزی برچسب‌های `P1.3.1` تا `P1.3.5`
- [x] بررسی فایل قفل
- [x] اجرای Prettier
- [x] اجرای ESLint
- [x] اجرای TypeScript
- [x] اجرای `44` آزمون در `9` فایل آزمون
- [x] Build تازه چهار برنامه با `0` نتیجه Cacheشده
- [x] Smoke هماهنگ
- [x] بررسی توقف و آزادشدن پورت‌های `3000` و `3001`
- [x] ممیزی UTF-8 و متن
- [x] گسترش نقشه راه تا `P12`
- [x] بازبینی فایل‌به‌فایل اسناد
- [x] ایجاد هر شش سند مرجع مفقود
- [x] جایگزینی نسخه‌های بازبینی‌شده اسناد
- [x] ممیزی دقیق `14` سند مرجع
- [x] تأیید اینکه فقط همان `14` مسیر مستنداتی تغییر کرده‌اند
- [x] اجرای دوباره `pnpm check` روی وضعیت نهایی
- [x] اجرای دوباره Build تازه چهار برنامه
- [x] اجرای دوباره Smoke هماهنگ
- [x] ثبت گزارش نهایی پذیرش
- [x] به‌روزرسانی وضعیت، نقشه راه، ریسک و ماتریس ردیابی

موارد باقی‌مانده:

- [x] ایجاد Commit رسمی پذیرش با همین تغییر
- [x] ایجاد Tag پذیرش `P1.3.6`
- [x] ایجاد Tag تکمیل مرحله مادر `P1.3`
- [x] تأیید فضای کاری پاک
- [x] انتقال رسمی به `P1.4.1`

## 56. Commit و Tag هدف برای بسته‌شدن

پیام Commit هدف:

```text
Stage P1.3.6: accept executable applications bootstrap
```

Tagهای هدف:

```text
stage-p1.3.6-executable-applications-acceptance
stage-p1.3-executable-applications-bootstrap-complete
```

در زمان تعریف این هدف، Commit و Tagها هنوز ایجاد نشده بودند و نباید در آن مقطع ثبت‌شده تلقی می‌شدند.

## 57. معیار نهایی بسته‌شدن P1.3.6

مرحله فقط زمانی بسته می‌شود که:

1. اسناد مرجع تأیید و جایگزین شوند.
2. تناقض میان کد، وضعیت، نقشه راه و Journal وجود نداشته باشد.
3. `pnpm check` روی وضعیت نهایی موفق باشد.
4. Build تازه چهار برنامه موفق باشد.
5. Smoke هماهنگ موفق باشد.
6. پورت‌ها پس از توقف آزاد باشند.
7. ممیزی UTF-8 موفق باشد.
8. نتیجه نهایی در Journal ثبت شود.
9. `PROJECT-STATUS.md` به `P1.4.1` منتقل شود.
10. ردیف‌های `P1.3.6` در `ROADMAP.md` بسته شوند.
11. Commit رسمی ایجاد شود.
12. دو Tag هدف ایجاد شوند.
13. `git status` پاک باشد.

## 58. نتیجه نهایی P1.3.6

- وضعیت: بسته‌شده
- پذیرش فنی نهایی: موفق
- پذیرش مستنداتی نهایی: موفق
- کنترل کامل کیفیت روی وضعیت نهایی: موفق
- Build تازه چهار برنامه با `0` نتیجه Cacheشده: موفق
- Smoke هماهنگ و پاک‌سازی فرایندها: موفق
- Commit پذیرش: `089a7066e31cd413cfce3a5246ee0038cc2e5e73`
- Tag پذیرش: `stage-p1.3.6-executable-applications-acceptance`
- Tag تکمیل مرحله مادر: `stage-p1.3-executable-applications-bootstrap-complete`
- فضای کاری پس از Commit و ایجاد Tagها: پاک
- مرحله جاری پس از انتقال: `P1.4.1 — ممیزی Docker Desktop و Docker Compose`

# وضعیت کلی پس از آخرین ثبت Journal

## 59. برنامه‌های اجرایی

- Web: Bootstrap بسته‌شده
- API: Bootstrap بسته‌شده
- Worker: Bootstrap بسته‌شده
- Scheduler: Bootstrap بسته‌شده
- اجرای هماهنگ: بسته‌شده
- پذیرش مرحله مادر: بسته‌شده

## 60. قابلیت‌های تجاری

موارد زیر هنوز پیاده‌سازی‌شده تلقی نمی‌شوند:

- پایگاه داده واقعی؛
- Migration؛
- RLS؛
- هویت و نشست؛
- سازمان و تیم؛
- نقش و مجوز؛
- پرونده و اقدام؛
- انتظار و تصمیم؛
- یادآور و اعلان واقعی؛
- Timeline و Audit تجاری؛
- فایل و اسکن بدافزار؛
- گزارش و Snapshot؛
- مخاطب، پروژه و جست‌وجو؛
- استقرار عملیاتی؛
- Backup و Restore.

## 61. اصل ادامه

`P1.4.1` با ممیزی واقعی محیط بسته شده و ادامه توسعه از `P1.4.2` انجام می‌شود.

دامنه `P1.4.2`:

- تعریف فایل‌های محیط محلی؛
- تعریف الگوهای امن Environment؛
- ثبت مقادیر نمونه غیرحساس؛
- تعریف قاعده نگهداری Secretهای واقعی خارج از Git؛
- جلوگیری از ثبت Credential واقعی در مخزن.

تا پیش از نصب و پذیرش Docker Desktop:

- اجرای Container امکان‌پذیر نیست؛
- Docker Context و Compose قابل آزمون نیستند؛
- PostgreSQL 16، Redis 7 و MinIO راه‌اندازی‌شده تلقی نمی‌شوند.

## 62. ممیزی نهایی بسته مستندات P1.3.6

ممیزی دقیق بسته مستندات روی وضعیت نهایی مخزن اجرا شد.

نتیجه:

- تعداد اسناد الزامی: `14`
- تعداد اسناد موجود: `14`
- تعداد اسناد موفق در ممیزی دقیق: `14`
- تعداد اسناد دارای Hash غیرمنتظره پیش از قالب‌بندی نهایی: `0`
- تعداد اسناد دارای UTF-8 نامعتبر: `0`
- تعداد اسناد دارای BOM: `0`
- تعداد اسناد دارای نویسه جایگزین Unicode: `0`
- تعداد اسناد دارای فاصله انتهای خط: `0`
- تعداد اسناد فاقد خط پایانی استاندارد: `0`
- نتیجه `git diff --check`: موفق
- تعداد مسیرهای تغییرکرده: `14`
- تعداد مسیرهای تغییرکرده غیرمنتظره: `0`

## 63. اجرای نهایی کنترل‌های فنی P1.3.6

کنترل‌های فنی پس از نهایی‌شدن مستندات دوباره اجرا شدند.

نتیجه:

- `pnpm format:check`: موفق
- ESLint با صفر Warning: موفق
- TypeScript: موفق
- تعداد فایل‌های آزمون: `9`
- تعداد آزمون‌ها: `44`
- تعداد آزمون‌های موفق: `44`
- Build اجباری چهار برنامه: موفق
- تعداد Build موفق: `4`
- تعداد Build Cacheشده در اجرای اجباری: `0`
- Smoke هماهنگ چهار برنامه: موفق
- Web روی درگاه `3000`: موفق
- API روی درگاه `3001`: موفق
- Worker Run Once: موفق
- Scheduler Run Once: موفق
- آزادشدن درگاه‌های `3000` و `3001`: موفق
- تغییر ناخواسته خارج از اسناد: مشاهده نشد

## 64. نتیجه پیش از Commit پذیرش — ثبت تاریخی

تمام معیارهای فنی و مستنداتی پیش از Commit انجام شده بودند.

وضعیت ثبت‌شده در آن مقطع:

- `P1.3.6.9` تا `P1.3.6.13`: انجام‌شده
- Commit پذیرش در آن مقطع: هنوز ایجاد نشده بود
- Tag پذیرش `P1.3.6` در آن مقطع: هنوز ایجاد نشده بود
- Tag تکمیل مرحله مادر `P1.3` در آن مقطع: هنوز ایجاد نشده بود
- فضای کاری در آن مقطع فقط شامل تغییرات مورد انتظار بسته مستندات بود.
- انتقال به `P1.4.1` تا پس از Commit، Tagها و تأیید Git پاک ممنوع بود.

## 65. بسته‌شدن رسمی P1.3.6 و انتقال به P1.4.1

شواهد نهایی:

- Commit پذیرش: `089a7066e31cd413cfce3a5246ee0038cc2e5e73`
- Tag پذیرش: `stage-p1.3.6-executable-applications-acceptance`
- Tag تکمیل مرحله مادر: `stage-p1.3-executable-applications-bootstrap-complete`
- هر دو Tag سبک و اشاره‌کننده به Commit پذیرش هستند.
- فضای کاری پس از Commit و ایجاد Tagها پاک بود.
- ردیف‌های `P1.3.6.14` تا `P1.3.6.17` تکمیل شدند.
- مرحله مادر `P1.3` رسماً بسته شد.
- مرحله جاری به `P1.4.1 — ممیزی Docker Desktop و Docker Compose` منتقل شد.
- `P1.4.1` فقط‌خواندنی است.
- هنوز هیچ سرویس زیرساختی ایجاد یا پیکربندی‌شده تلقی نمی‌شود.

## 66. نتیجه ممیزی P1.4.1 و انتقال به P1.4.2

- وضعیت `P1.4.1`: بسته‌شده
- نوع اجرا: ممیزی واقعی و فقط‌خواندنی
- مرحله جاری: `P1.4.2 — تعریف فایل‌های محیط محلی و مقادیر نمونه امن`
- مرحله بعدی: `P1.4.3 — راه‌اندازی PostgreSQL 16 با نسخه ثابت`

یافته‌های نهایی:

- Docker Desktop نصب نبود.
- Docker CLI و Docker Compose در دسترس نبودند.
- Docker Context و اجرای Container قابل آزمون نبودند.
- WSL `2` با توزیع Ubuntu در دسترس بود.
- پورت‌های `3000`، `3001`، `5432`، `6379`، `9000` و `9001` آزاد شدند.
- فضای آزاد درایو `C:` به `25.68 GB` رسید.
- هیچ Docker Volume، Network یا مسیر ذخیره‌سازی موجودی یافت نشد.
- هیچ فایل Docker، Compose یا Environment در مخزن وجود نداشت.
- هیچ متغیر محیطی مرتبط با Credential یا زیرساخت یافت نشد.
- PostgreSQL `18.4` متعارض به‌طور کامل حذف شد.
- سرویس `postgresql-x64-18` حذف شد.
- مسیر `C:\Program Files\PostgreSQL\18` حذف شد.
- پورت `5432` پس از حذف PostgreSQL آزاد بود.
- فضای کاری Git در پایان ممیزی پاک باقی ماند.

نتیجه:

`P1.4.1` با ثبت وضعیت واقعی محیط بسته شد. نبود Docker یک یافته رسمی ممیزی است و نصب آن باید پیش از اجرای سرویس‌های Containerشده انجام شود. `P1.4.2` فقط به تعریف فایل‌های محیط محلی و نمونه‌های امن اختصاص دارد و نباید شامل Secret یا Credential واقعی باشد.

## 67. بسته‌شدن P1.4.2 و انتقال به P1.4.3

- وضعیت `P1.4.2`: بسته‌شده
- هدف: تعریف فایل‌های محیط محلی و مقادیر نمونه امن
- فایل ثبت‌شده در Git: `.env.example`
- فایل واقعی محلی: `.env.local`
- وضعیت `.env.local`: خارج از Git و مشمول قاعده `.env.*`
- تعداد مقادیر نمونه غیرحساس: `9`
- Secret یا Credential واقعی ثبت‌شده: `0`
- Dependency جدید: اضافه نشد
- سرویس یا Container ایجادشده: `0`

اصلاح اصلی:

- توضیحات و عنوان‌های `.env.example` به Comment معتبر تبدیل شدند.
- مقادیر اجرایی مصوب بدون تغییر حفظ شدند.
- `.env.local` با الگوی امن همگام شد و Hash دو فایل یکسان بود.
- فایل واقعی محلی در Git ثبت نشد.

شواهد آزمون و کیفیت:

- آزمون متمرکز Environment: `4/4` موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `10`
- تعداد آزمون‌های موفق: `48/48`
- `git diff --check`: موفق
- فضای کاری پس از Commit اجرایی و کنترل کامل: پاک

ثبت Git:

- Commit اجرایی: `179cc504272fdae80da22bfb7abfb1ab582dc313`
- پیام Commit: `Stage P1.4.2: finalize safe local environment template`
- Tag اختصاصی برای `P1.4.2` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.3 — راه‌اندازی PostgreSQL 16 با نسخه ثابت`

قید اجرایی مرحله بعد:

Docker Desktop، Docker CLI و Docker Compose هنوز نصب نیستند. نخستین اقدام اجرایی `P1.4.3` نصب و پذیرش Docker Desktop است. PostgreSQL 16 فقط پس از موفقیت Docker Engine، Docker Compose و اجرای یک Container آزمایشی راه‌اندازی می‌شود.

## 68. بسته‌شدن P1.4.3 و انتقال به P1.4.4

- وضعیت `P1.4.3`: بسته‌شده
- هدف: نصب و پذیرش Docker Desktop و راه‌اندازی PostgreSQL 16 با نسخه ثابت
- Docker Desktop: نسخه `4.84.0`
- Docker Engine: نسخه `29.6.2`
- Docker Compose: نسخه `5.3.1`
- Docker Context: `desktop-linux`
- آزمون Container عمومی: `hello-world` موفق
- تصویر PostgreSQL: `postgres:16.14-bookworm`
- Digest معماری `linux/amd64`: `sha256:c95fd5346040eba2de3c435e14874af18f5d681fb5848d4f081dbead0878af28`
- نام Container: `orgawork-postgres`
- نسخه واقعی Server: `16.14`
- پایگاه داده و کاربر: `orgawork|orgawork`
- Endpoint محلی: `127.0.0.1:5432`
- Network Mode موقت: `bridge`
- ذخیره‌سازی مرحله جاری: `tmpfs` و غیرپایدار
- Docker Volume پایدار ایجادشده: `0`
- Secret واقعی: فقط در `.env.local` و خارج از Git
- فایل Compose: `infra/compose/postgresql.compose.yaml`

شواهد آزمون و پذیرش:

- Docker CLI، Engine، Compose و Context فعال: موفق
- اجرای `hello-world`: موفق
- Query واقعی PostgreSQL: `160014|orgawork|orgawork`
- اتصال TCP میزبان به پورت `5432`: موفق
- کنترل تصویر و Digest ثابت: موفق
- بررسی نبود Volume پایدار زودهنگام: موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `10`
- تعداد آزمون‌های موفق: `48/48`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `b20f68df858ed4568d2e3fdb3d414491a4276445`
- پیام Commit: `Stage P1.4.3: add pinned PostgreSQL 16 service`
- Tag اختصاصی برای `P1.4.3` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.4 — راه‌اندازی Redis 7 با نسخه ثابت`

قید ادامه:

PostgreSQL 16.14 در زمان بسته‌شدن این زیرمرحله فعال است، اما داده آن عمداً روی `tmpfs` قرار دارد. شبکه داخلی و Volume پایدار فقط در `P1.4.6` تعریف می‌شود. مرحله جاری بعدی فقط به راه‌اندازی Redis 7 با نسخه ثابت اختصاص دارد.

## 69. بسته‌شدن P1.4.4 و انتقال به P1.4.5

- وضعیت `P1.4.4`: بسته‌شده
- هدف: راه‌اندازی Redis 7 با نسخه و Digest ثابت و پذیرش فنی واقعی
- تصویر Redis: `redis:7.4.10-bookworm`
- Digest معماری `linux/amd64`: `sha256:fe24fa2bcb59930f8863cf36a472df24efaccd8be4ee98ffe528f06d57d68dc2`
- نام Container: `orgawork-redis`
- Compose Project مستقل: `orgawork-redis-local`
- نسخه واقعی Redis: `7.4.10`
- Endpoint محلی: `127.0.0.1:6379`
- Network Mode موقت: `bridge`
- ذخیره‌سازی مرحله جاری: `tmpfs` و غیرپایدار
- Snapshot زمان‌بندی‌شده: غیرفعال
- AOF: غیرفعال
- Docker Volume پایدار ایجادشده: `0`
- Secret واقعی: فقط در `.env.local` و خارج از Git
- فایل Compose: `infra/compose/redis.compose.yaml`
- وضعیت PostgreSQL هم‌زمان: `160014|orgawork|orgawork`

شواهد آزمون و پذیرش:

- `PING` احرازشده: `PONG`
- دسترسی بدون رمز: با `NOAUTH` رد شد
- اتصال احرازشده از Endpoint میزبان: موفق
- کنترل نسخه و Digest ثابت: موفق
- کنترل نبود Volume پایدار زودهنگام: موفق
- کنترل غیرفعال‌بودن Snapshot و AOF: موفق
- حذف هشدار Orphan با جداسازی Compose Project: موفق
- سلامت هم‌زمان PostgreSQL 16.14: موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `10`
- تعداد آزمون‌های موفق: `48/48`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `4a1f7fba8d625a26c7faf19c7e20ae35264759c0`
- پیام Commit: `Stage P1.4.4: add pinned Redis 7 service`
- Tag اختصاصی برای `P1.4.4` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.5 — راه‌اندازی MinIO با نسخه ثابت`

قید ادامه:

PostgreSQL 16.14 و Redis 7.4.10 در زمان بسته‌شدن این زیرمرحله فعال‌اند، اما داده هر دو عمداً روی `tmpfs` قرار دارد. شبکه داخلی و Volumeهای پایدار فقط در `P1.4.6` تعریف می‌شوند. مرحله جاری بعدی فقط به راه‌اندازی MinIO با نسخه و Digest ثابت اختصاص دارد.

## 70. بسته‌شدن P1.4.5 و انتقال به P1.4.6

- وضعیت `P1.4.5`: بسته‌شده
- هدف: راه‌اندازی MinIO با نسخه و Digest ثابت و پذیرش فنی واقعی S3
- تصویر MinIO: `minio/minio:RELEASE.2025-09-07T16-13-09Z`
- Digest معماری `linux/amd64`: `sha256:a1a8bd4ac40ad7881a245bab97323e18f971e4d4cba2c2007ec1bedd21cbaba2`
- نام Container: `orgawork-minio`
- Compose Project مستقل: `orgawork-minio-local`
- Compose Service: `minio`
- نسخه واقعی MinIO: `RELEASE.2025-09-07T16-13-09Z`
- Endpoint محلی API: `127.0.0.1:9000`
- Endpoint محلی Console: `127.0.0.1:9001`
- Network Mode موقت: `bridge`
- ذخیره‌سازی مرحله جاری: `tmpfs` روی `/data` و غیرپایدار
- Bind Mount ایجادشده: `0`
- Persistent Mount ایجادشده: `0`
- Docker Volume پایدار ایجادشده: `0`
- Credential واقعی: فقط در `.env.local` و خارج از Git
- فایل Compose: `infra/compose/minio.compose.yaml`
- وضعیت PostgreSQL هم‌زمان: `160014|orgawork|orgawork`
- وضعیت Redis هم‌زمان: `PONG`

شواهد آزمون و پذیرش:

- کنترل تصویر و Digest ثابت معماری `linux/amd64`: موفق
- کنترل نسخه واقعی داخل Container: موفق
- مسیر سلامت API: HTTP `200`
- دسترسی بدون احراز هویت به S3: با HTTP `403` رد شد
- درخواست احرازشده `ListBuckets` با AWS Signature Version 4: HTTP `200`
- پاسخ XML احرازشده: `ListAllMyBucketsResult`
- دسترسی به Console: HTTP `200`
- کنترل انتشار API و Console فقط روی `127.0.0.1`: موفق
- کنترل `Binds=null`: موفق
- کنترل `tmpfs` روی `/data`: موفق
- کنترل نبود Persistent Mount و Docker Volume: موفق
- سلامت هم‌زمان PostgreSQL 16.14: موفق
- سلامت هم‌زمان Redis 7.4.10: موفق
- کنترل نبود Credential واقعی در Diff و Stage: موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `10`
- تعداد آزمون‌های موفق: `48/48`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `c95a5a527df830cd2847df8d17b11320fe34d061`
- پیام Commit: `Stage P1.4.5: add pinned MinIO service`
- Tag اختصاصی برای `P1.4.5` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.6 — تعریف شبکه داخلی و Volumeهای پایدار`

قید ادامه:

PostgreSQL 16.14، Redis 7.4.10 و MinIO در زمان بسته‌شدن این زیرمرحله فعال‌اند، اما داده هر سه سرویس عمداً غیرپایدار است. شبکه داخلی اختصاصی و Volumeهای پایدار فقط در `P1.4.6` تعریف می‌شوند. Healthcheck و Readiness رسمی همچنان متعلق به `P1.4.7` است.

## 71. بسته‌شدن P1.4.6 و انتقال به P1.4.7

- وضعیت `P1.4.6`: بسته‌شده
- هدف: تعریف شبکه اختصاصی مشترک و Docker Volumeهای نام‌دار پایدار برای PostgreSQL، Redis و MinIO
- Compose Project مشترک: `orgawork-data-local`
- شبکه اختصاصی: `orgawork-internal`
- Network Driver: `bridge`
- Network Internal: `false` برای حفظ انتشار Endpointهای محلی در Docker Desktop
- Gateway پذیرفته‌شده: `172.18.0.1`
- تعداد اعضای فعال شبکه: `3`
- Volume PostgreSQL: `orgawork-postgres-data` روی `/var/lib/postgresql/data`
- Volume Redis: `orgawork-redis-data` روی `/data`
- Volume MinIO: `orgawork-minio-data` روی `/data`
- Bind Mount داده: `0`
- tmpfs باقی‌مانده: `0`
- Redis Persistence: `appendonly=yes` و `appendfsync=everysec`
- Endpoint PostgreSQL: `127.0.0.1:5432`
- Endpoint Redis: `127.0.0.1:6379`
- Endpoint MinIO API: `127.0.0.1:9000`
- Endpoint MinIO Console: `127.0.0.1:9001`
- تصاویر، Digestها، Credentialها و نسخه‌های پذیرفته‌شده بدون تغییر حفظ شدند.

شواهد آزمون و پذیرش:

- اعتبارسنجی ترکیبی سه فایل Compose: موفق
- کنترل شبکه اختصاصی مشترک و سه عضو فعال: موفق
- کنترل Gateway واقعی شبکه: موفق
- کنترل اتصال هر سه Container به `orgawork-internal`: موفق
- کنترل نبود `tmpfs` در هر سه سرویس: موفق
- کنترل Mountهای نوع `volume` و نام‌های صریح: موفق
- کنترل نبود Bind Mount میزبان: موفق
- کنترل انتشار همه Portها فقط روی `127.0.0.1`: موفق
- کنترل هویت PostgreSQL: `160014|orgawork|orgawork`
- کنترل Redis: `PONG`
- کنترل AOF Redis: `appendonly=yes` و `appendfsync=everysec`
- مسیر سلامت MinIO: HTTP `200`
- درخواست S3 بدون احراز هویت: HTTP `403`
- درخواست احرازشده `ListBuckets`: HTTP `200` و XML معتبر `ListAllMyBucketsResult`
- کنترل DNS داخلی برای `redis` و `minio`: موفق
- کنترل اتصال TCP داخلی PostgreSQL به Redis و MinIO: موفق
- آزمون متمرکز قرارداد زیرساخت: `4/4` موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `11`
- تعداد آزمون‌های موفق: `52/52`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `eb54b2bbf9f5e6668dd4f1ea1ee53159cc09ac94`
- پیام Commit: `Stage P1.4.6: add dedicated network and persistent volumes`
- Tag اختصاصی برای `P1.4.6` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.7 — تعریف بررسی سلامت و آمادگی سرویس‌ها`

قید ادامه:

Healthcheck و Readiness رسمی سرویس‌ها فقط در `P1.4.7` تعریف می‌شوند. ایجاد خودکار Bucket خصوصی فایل‌ها متعلق به `P1.4.8` است. آزمون رسمی ماندگاری داده پس از Restart مطابق Roadmap در `P1.4.11` انجام می‌شود.

## 72. بسته‌شدن P1.4.7 و انتقال به P1.4.8

- وضعیت `P1.4.7`: بسته‌شده
- هدف: تعریف و پذیرش رسمی Healthcheck و Readiness سرویس‌های PostgreSQL، Redis و MinIO
- Compose Project مشترک: `orgawork-data-local`
- شبکه اختصاصی حفظ‌شده: `orgawork-internal`
- Volume PostgreSQL حفظ‌شده: `orgawork-postgres-data`
- Volume Redis حفظ‌شده: `orgawork-redis-data`
- Volume MinIO حفظ‌شده: `orgawork-minio-data`
- زمان‌بندی مشترک Healthcheckها: `interval=10s`، `timeout=5s`، `retries=5` و `start_period=20s`
- Healthcheck PostgreSQL: `pg_isready` با `POSTGRES_USER` و `POSTGRES_DB`
- Healthcheck Redis: `redis-cli` احرازشده با `REDIS_PASSWORD` و انتظار پاسخ دقیق `PONG`
- Healthcheck MinIO: Endpoint داخلی `/minio/health/ready`
- Credentialهای واقعی فقط در فایل محلی و ignored `.env.local` باقی ماندند.

شواهد آزمون و پذیرش:

- اعتبارسنجی ترکیبی سه فایل Compose: موفق
- بازسازی هر سه Container با قرارداد Healthcheck جدید: موفق
- وضعیت نهایی PostgreSQL: `healthy`
- وضعیت نهایی Redis: `healthy`
- وضعیت نهایی MinIO: `healthy`
- Failing Streak هر سه سرویس: `0`
- آخرین Exit Code هر سه Healthcheck: `0`
- عملیات آمادگی PostgreSQL: `pg_isready` و `SELECT 1` موفق
- هویت PostgreSQL: `160014|orgawork|orgawork`
- عملیات آمادگی Redis: `PING` احرازشده با پاسخ `PONG`
- درخواست Redis بدون احراز هویت: `NOAUTH Authentication required.`
- Endpoint آمادگی MinIO: HTTP `200`
- درخواست احرازشده `ListBuckets`: HTTP `200` و XML معتبر `ListAllMyBucketsResult`
- شبکه، Port Bindingها و Volumeهای نام‌دار: بدون تغییر
- آزمون متمرکز قرارداد Healthcheck و Readiness: `4/4` موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `12`
- تعداد آزمون‌های موفق: `56/56`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `dc29514df161c4f10cb5ff58e2b0a06c5617ad82`
- پیام Commit: `Stage P1.4.7: add service health and readiness checks`
- Tag اختصاصی برای `P1.4.7` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.8 — ایجاد خودکار Bucket خصوصی فایل‌ها`

قید ادامه:

ایجاد خودکار Bucket خصوصی فایل‌ها فقط در `P1.4.8` انجام می‌شود. فرمان‌های یکپارچه آغاز، توقف، گزارش و پاک‌سازی زیرساخت متعلق به `P1.4.9` است. آزمون رسمی ماندگاری داده پس از Restart مطابق Roadmap در `P1.4.11` انجام می‌شود.

## 73. بسته‌شدن P1.4.8 و انتقال به P1.4.9

- وضعیت `P1.4.8`: بسته‌شده
- هدف: ایجاد خودکار، تکرارپذیر و امن Bucket خصوصی فایل‌ها در MinIO
- نام Bucket: `orgawork-files`
- متغیر محیطی غیرحساس: `MINIO_BUCKET=orgawork-files`
- سرویس یک‌باره Compose: `minio_bucket_init`
- Container: `orgawork-minio-bucket-init`
- Image و Digest: همان نسخه ثابت‌شده MinIO
- وابستگی اجرا: `condition: service_healthy` برای سرویس MinIO
- شبکه اختصاصی: `orgawork-internal`
- Restart Policy: `no`
- ایجاد Alias داخلی احرازشده: `mc alias set`
- ایجاد idempotent: `mc mb --ignore-existing`
- تحمیل Policy خصوصی: `mc anonymous set private`
- کنترل وجود Bucket: `mc stat`
- Credentialهای واقعی فقط در فایل محلی و ignored `.env.local` باقی ماندند.

شواهد آزمون و پذیرش:

- اعتبارسنجی ترکیبی سه فایل Compose: موفق
- اجرای اولیه Initializer: Exit Code برابر `0`
- دو اجرای مجدد idempotent Initializer: `2/2` موفق
- تعداد کل اجرای پذیرفته‌شده Initializer: `3`
- تعداد نهایی Bucketها پس از سه اجرا: `1`
- Bucket نهایی: `orgawork-files`
- فهرست‌گیری احرازشده Bucket: HTTP `200`
- دسترسی بدون احراز هویت به Bucket: HTTP `403`
- Policy ناشناس Bucket: `private`
- Container اصلی MinIO بازسازی نشد.
- شناسه Container، Image و Digest، شبکه، Volume، Port Bindingها و Healthcheck MinIO بدون تغییر باقی ماندند.
- وضعیت نهایی PostgreSQL: `healthy`
- وضعیت نهایی Redis: `healthy`
- وضعیت نهایی MinIO: `healthy`
- آزمون متمرکز قرارداد Bucket و محیط: `9/9` موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `13`
- تعداد آزمون‌های موفق: `61/61`
- `git diff --check`: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `96126445f2ec1c6ba174767f75b062b46e5663fe`
- پیام Commit: `Stage P1.4.8: add automatic private MinIO bucket initialization`
- Tag اختصاصی برای `P1.4.8` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.9 — افزودن فرمان‌های آغاز، توقف، گزارش و پاک‌سازی زیرساخت`

قید ادامه:

فرمان‌های یکپارچه آغاز، توقف، گزارش و پاک‌سازی زیرساخت فقط در `P1.4.9` تعریف می‌شوند. آزمون اتصال واقعی برنامه‌ها به سرویس‌های محلی متعلق به `P1.4.10` و آزمون رسمی ماندگاری داده پس از Restart متعلق به `P1.4.11` است.

## 74. بسته‌شدن P1.4.9 و انتقال به P1.4.10

- وضعیت `P1.4.9`: بسته‌شده
- هدف: افزودن فرمان‌های یکپارچه، امن و قابل‌آزمون برای آغاز، توقف، گزارش و پاک‌سازی زیرساخت محلی داده
- Compose Project ثابت: `orgawork-data-local`
- فایل محیط محلی: `.env.local`
- فایل‌های Compose مدیریت‌شده: `postgresql.compose.yaml`، `redis.compose.yaml` و `minio.compose.yaml`
- فرمان آغاز: `pnpm infra:start`
- فرمان توقف: `pnpm infra:stop`
- فرمان گزارش: `pnpm infra:report`
- فرمان پاک‌سازی: `pnpm infra:cleanup`
- برنامه مرکزی فرمان‌ها: `tools/scripts/local-infrastructure-plan.ts`
- اجرای واقعی فرمان‌ها: `tools/scripts/local-infrastructure.ts`
- آزمون قرارداد فرمان‌ها: `tools/checks/local-infrastructure-commands.test.ts`
- حذف Volume با `--volumes` یا `-v`: ممنوع
- استفاده از `--remove-orphans`: ممنوع

شواهد آزمون و پذیرش:

- فرمان `infra:report`: موفق و کاملاً فقط‌خواندنی
- تغییر Containerها توسط گزارش: صفر
- تغییر شبکه و Volumeها توسط گزارش: صفر
- تغییر فضای کاری توسط گزارش: صفر
- فرمان `infra:stop`: موفق
- Containerهای متوقف‌شده: `4/4`
- Containerهای حذف‌شده در توقف: `0`
- شناسه Containerها پس از توقف: بدون تغییر
- شبکه اختصاصی پس از توقف: حفظ‌شده
- Volumeهای پایدار پس از توقف: `3/3` حفظ‌شده
- اجرای `infra:start` پس از توقف: موفق
- سرویس‌های اصلی سالم پس از آغاز: `3/3`
- شناسه Containerهای اصلی پس از آغاز: بدون تغییر
- Initializer پس از آغاز: Exit Code برابر `0` و Restart Policy برابر `no`
- فرمان `infra:cleanup`: چهار Container و شبکه Compose را حذف کرد
- Volumeهای پایدار پس از پاک‌سازی: `3/3` حفظ‌شده
- هویت Volumeهای پایدار پس از پاک‌سازی: بدون تغییر
- علت خطای گزارش پس از پاک‌سازی: تطبیق حساس به بزرگی و کوچکی حروف در پیام `no such object`
- اصلاح خطا: تطبیق case-insensitive پیام نبود Container
- اجرای مجدد `infra:cleanup` در وضعیت ازپیش‌پاک‌شده: موفق
- پذیرش idempotent پاک‌سازی: موفق
- اجرای تازه `infra:start` پس از پاک‌سازی کامل: موفق
- Containerهای ایجادشده پس از آغاز تازه: `4/4`
- شبکه `orgawork-internal`: دوباره ایجاد شد و Label پروژه صحیح باقی ماند
- سرویس‌های اصلی پس از آغاز تازه: PostgreSQL، Redis و MinIO همگی `healthy`
- Initializer پس از آغاز تازه: Exit Code برابر `0`
- Bucket نهایی: دقیقاً یک Bucket با نام `orgawork-files`
- دسترسی احرازشده Bucket: HTTP `200`
- دسترسی بدون احراز هویت Bucket: HTTP `403`
- Policy Bucket: `private`
- آزمون متمرکز قرارداد فرمان‌های زیرساخت: `8/8` موفق
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `14`
- تعداد آزمون‌های موفق: `69/69`
- `git diff --check`: موفق
- بررسی Secretهای محلی در Diff و Staging: موفق
- فضای کاری پس از Commit فنی: پاک

ثبت Git:

- Commit فنی: `061bbe20b0acda86d1aca2ac786cb076c98e5690`
- پیام Commit: `Stage P1.4.9: add unified local infrastructure commands`
- Tag اختصاصی برای `P1.4.9` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.10 — آزمون اتصال واقعی برنامه‌ها به سرویس‌های محلی`

قید ادامه:

آزمون اتصال واقعی برنامه‌های Web، API، Worker و Scheduler به PostgreSQL، Redis و MinIO فقط در `P1.4.10` انجام می‌شود. آزمون رسمی ماندگاری محتوای داده پس از توقف و آغاز مجدد متعلق به `P1.4.11` است. هیچ Credential یا Secret واقعی نباید در Git ثبت شود.

## 75. بسته‌شدن P1.4.10 و انتقال به P1.4.11

- وضعیت `P1.4.10`: بسته‌شده
- هدف: اثبات اتصال واقعی Web، API، Worker و Scheduler به PostgreSQL، Redis و MinIO محلی با عملیات فقط‌خواندنی و خروجی پاک‌سازی‌شده
- دامنه عملیات: `SELECT 1` برای PostgreSQL، `PING` برای Redis و `HeadBucket` برای MinIO
- Bucket هدف MinIO: `orgawork-files` با Policy خصوصی
- ثبت Secret واقعی در Git یا خروجی آزمون: ممنوع

خروجی‌های فنی:

- بسته تنظیمات مشترک: `packages/configuration`
- بسته اتصال PostgreSQL: `packages/database`
- بسته اتصال Redis: `packages/queue`
- بسته اتصال MinIO: `packages/storage`
- مسیر اتصال API: `GET /connectivity`
- مسیر اتصال Web: `GET /api/connectivity`
- رخداد آغاز Worker و Scheduler: `connectivity-verified`
- قرارداد نسخه‌ها: `tools/checks/application-connectivity-plan.ts`
- آزمون قرارداد: `tools/checks/application-connectivity-contract.test.ts`

شواهد آزمون و پذیرش:

- آزمون قرارداد اتصال: `6/6` موفق
- آزمون تنظیمات مشترک: `4/4` موفق
- پذیرش واقعی API، Worker، Scheduler و Web در برابر هر سه سرویس: موفق
- عملیات تغییردهنده داده در پذیرش واقعی: صفر
- Secretهای چاپ‌شده در پذیرش واقعی: صفر
- کنترل کامل `pnpm check`: موفق
- تعداد فایل‌های آزمون: `20`
- تعداد آزمون‌های موفق: `86/86`
- ساخت Turbo برای چهار برنامه و چهار بسته وابسته: `8/8` موفق
- `git diff --check`: موفق
- `pnpm-workspace.yaml`: بدون تغییر
- `.env.local`: ignored و ثبت‌نشده در Git

خطاها و اصلاحات ثبت‌شده:

- اجرای نخست پذیرش واقعی به‌دلیل Top-level Await در فایل موقت CommonJS متوقف شد؛ Importها به تابع `async` منتقل و پذیرش واقعی موفق شد.
- Closure نخست وجود اسکریپت ریشه‌ای `build` را به‌اشتباه فرض کرد؛ فرمان‌های واقعی `pnpm check` و `pnpm build:apps` اجرا شدند.
- بررسی پس از Commit به‌دلیل تفاوت جداکننده مسیر Windows و Git توقف کرد؛ دامنه Commit با مسیرهای نرمال‌شده دوباره بررسی و دقیقاً `40` فایل تأیید شد.

ثبت Git:

- Commit فنی: `707f3f855ccd3fe618d4fcf6d95daa2022143680`
- پیام Commit: `P1.4.10: connect applications to local data services`
- فایل‌های Commit فنی: `40` فایل دقیق
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.11 — آزمون پایداری داده پس از توقف و آغاز مجدد`

قید ادامه:

در `P1.4.11` باید محتوای واقعی و قابل‌شناسایی در PostgreSQL، Redis و MinIO ایجاد شود، سرویس‌ها با فرمان پذیرفته‌شده متوقف و دوباره آغاز شوند و حفظ همان محتوا بدون حذف یا بازسازی Volumeها اثبات شود. استفاده از `infra:cleanup`، حذف Volume یا تغییر Policy خصوصی Bucket مجاز نیست.

## 76. بسته‌شدن P1.4.11 و انتقال به P1.4.12

- وضعیت `P1.4.11`: بسته‌شده
- هدف: اثبات ماندگاری محتوای واقعی و قابل‌شناسایی PostgreSQL، Redis و MinIO پس از توقف و آغاز مجدد کنترل‌شده
- فرمان پذیرش: `pnpm infra:persistence`
- چرخه عمر مجاز: `pnpm infra:stop` و `pnpm infra:start`
- استفاده از `infra:cleanup`، حذف Volume یا گزینه‌های مخرب: انجام نشد
- Policy مربوط به Bucket `orgawork-files`: خصوصی باقی ماند
- محتوای پیش از توقف و پس از آغاز مجدد: `3/3` سرویس تأیید شد
- هویت Volumeهای پایدار: `3/3` حفظ شد
- پاک‌سازی داده و ساختارهای موقت پذیرش: تأیید شد
- آزمون متمرکز قرارداد و Regression: `8/8` موفق
- کنترل کامل پروژه: `21` فایل آزمون و `94/94` آزمون موفق
- ساخت برنامه‌ها و وابستگی‌ها: `8/8` Task موفق
- Commit فنی: `4d19216c8316d108e4ebf797b52abd132e3526c2`
- پیام Commit: `P1.4.11: verify local data persistence across restart`
- فایل‌های Commit فنی: `4` فایل دقیق
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.4.12 — آزمون و پذیرش مرحله`

اصلاح‌های تثبیت‌شده:

- متغیرهای Shell و Credentialهای محیطی MinIO در فرمان احراز هویت بازیابی شدند.
- اجرای `pnpm.cmd` در ویندوز از مسیر کنترل‌شده `cmd.exe` انجام شد.
- برای هر دو نقص، آزمون Regression ثبت شد.

قید ادامه:

`P1.4.12` فقط به پذیرش نهایی مرحله مادر `P1.4`، تطبیق شواهد `P1.4.1` تا `P1.4.11` و انتقال رسمی به `P1.5.1` اختصاص دارد. تا آن زمان مرحله مادر `P1.4` در حال اجرا باقی می‌ماند.

## 77. بسته‌شدن P1.4.12 و مرحله مادر P1.4

- وضعیت `P1.4.12`: بسته‌شده
- وضعیت مرحله مادر `P1.4 — زیرساخت محلی داده و ذخیره‌سازی`: بسته و پذیرفته‌شده
- دامنه پذیرش: تطبیق نهایی شواهد `P1.4.1` تا `P1.4.11` با مخزن و Runtime واقعی
- قابلیت اجرایی جدید در `P1.4.12`: افزوده نشد

شواهد پذیرش مرحله مادر:

- همه زیرمرحله‌های `P1.4.1` تا `P1.4.11` در Roadmap بسته بودند.
- شواهد ردیابی `EVD-009` تا `EVD-019` موجود و معتبر بودند.
- PostgreSQL، Redis و MinIO در وضعیت سالم `3/3` قرار داشتند.
- Initializer یک‌باره MinIO با کد خروج `0` خاتمه یافته بود.
- شبکه `orgawork-internal` و سه Volume پایدار موجود و سالم بودند.
- Bucket `orgawork-files` خصوصی باقی ماند.
- جدول، کلید و شیء موقت پذیرش باقی‌مانده: صفر `3/3`.
- کنترل کامل پروژه: `21` فایل آزمون و `94/94` آزمون موفق.
- ساخت چهار برنامه و چهار بسته وابسته: `8/8` Task موفق.
- Worktree پیش و پس از پذیرش پاک باقی ماند.

ثبت انتقال:

- Commit فنی پایه ماندگاری: `4d19216c8316d108e4ebf797b52abd132e3526c2`
- Commit اختتام `P1.4.11`: `6a23056d9f91ef4e67d7b60f5a2dcc78d601be5e`
- Tag اختصاصی برای `P1.4.12` یا مرحله مادر `P1.4` از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.5.1 — تکمیل بسته مشترک دسترسی به پایگاه داده`

قید ادامه:

`P1.5.1` فقط به تکمیل قرارداد و سطح دسترسی بسته مشترک پایگاه داده اختصاص دارد. اجراکننده مهاجرت‌های نسخه‌دار متعلق به `P1.5.2` است و نباید زودتر وارد دامنه شود.

## 78. بسته‌شدن P1.5.1 و انتقال به P1.5.2

- وضعیت `P1.5.1`: بسته‌شده
- هدف: تکمیل بسته مشترک دسترسی به PostgreSQL با Pool محدود، Query پارامتری، Transaction امن و خطاهای پایدار
- بسته هدف: `@workspace/database`
- قابلیت Migration نسخه‌دار: عمداً وارد دامنه نشد و به `P1.5.2` واگذار شد

خروجی‌های فنی:

- ساخت تنظیمات Pool با حد اتصال، Timeoutهای مشخص و نام برنامه قابل‌کنترل
- اجرای Query پارامتری از طریق Pool مشترک
- اجرای Transaction با `BEGIN`، `COMMIT` و `ROLLBACK` و آزادسازی قطعی Client
- خطاهای پایدار و بدون افشای Credential با کدهای `ACCESS_CLOSED`، `QUERY_FAILED`، `TRANSACTION_FAILED` و `CLOSE_FAILED`
- بستن idempotent Pool و جلوگیری از عملیات پس از بسته‌شدن
- Probe خواندنی قبلی PostgreSQL بدون تغییر رفتار حفظ شد

شواهد پذیرش:

- آزمون متمرکز بسته پایگاه داده: `8/8` موفق
- Query پارامتری واقعی روی PostgreSQL: موفق
- Transaction واقعی روی PostgreSQL: موفق
- کنترل کامل پروژه: `22` فایل آزمون و `102/102` آزمون موفق
- ساخت چهار برنامه و چهار بسته وابسته: `8/8` Task موفق
- Worktree پس از ثبت: پاک

ثبت Git:

- Commit فنی: `ccf4f811b85a0c8a6d94830acdb413ec0cb3df94`
- پیام Commit: `P1.5.1: complete shared PostgreSQL access package`
- فایل‌های Commit فنی: `2` فایل دقیق
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.5.2 — ایجاد اجراکننده مهاجرت‌های نسخه‌دار`

قید ادامه:

`P1.5.2` فقط به ایجاد اجراکننده مهاجرت‌های نسخه‌دار اختصاص دارد. ثبت تاریخچه، ترتیب و اثرانگشت مهاجرت‌ها متعلق به `P1.5.3` است و نباید زودتر وارد دامنه شود.

## 79. بسته‌شدن P1.5.2 و انتقال به P1.5.3

- وضعیت `P1.5.2`: بسته‌شده
- هدف: ایجاد اجراکننده کنترل‌شده Migrationهای SQL نسخه‌دار
- بسته هدف: `@workspace/database`
- دامنه خارج‌شده: ثبت پایدار تاریخچه، ترتیب و اثرانگشت Migrationها که به `P1.5.3` واگذار شد

خروجی‌های فنی:

- کشف فایل‌های SQL نسخه‌دار و نادیده‌گرفتن فایل‌های نامرتبط
- اعتبارسنجی نام فایل، شماره نسخه یکتا و متن SQL غیرخالی
- مرتب‌سازی قطعی Migrationها بر اساس نسخه صعودی
- فیلتر Migrationهای اعمال‌شده از طریق قرارداد `appliedVersions`
- اجرای Migrationهای باقی‌مانده در یک Transaction اتمیک
- عدم آغاز Transaction هنگام نبود Migration در انتظار
- خطاهای پایدار و بدون افشای SQL یا Credential
- Export عمومی اجراکننده از بسته `@workspace/database`

شواهد پذیرش:

- آزمون متمرکز اجراکننده: `8/8` موفق
- کشف نسخه‌ها در Runtime واقعی PostgreSQL: تأییدشده
- ترتیب قطعی در Runtime واقعی: تأییدشده
- اجرای Transactional در Runtime واقعی: تأییدشده
- قرارداد Skip برای نسخه‌های اعمال‌شده: تأییدشده
- کنترل کامل پروژه: `23` فایل آزمون و `110/110` آزمون موفق
- ساخت چهار برنامه و چهار بسته وابسته: `8/8` Task موفق
- Worktree پس از ثبت: پاک

ثبت Git:

- Commit فنی: `c5fc997944ebade88f11cd42a2203b64e09e8da7`
- پیام Commit: `P1.5.2: add versioned migration runner`
- فایل‌های Commit فنی: `3` فایل دقیق
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.5.3 — ثبت تاریخچه، ترتیب و اثرانگشت مهاجرت‌ها`

قید ادامه:

`P1.5.3` به ثبت پایدار تاریخچه، ترتیب و اثرانگشت Migrationها اختصاص دارد. تغییر Schema دامنه، RLS و داده تجاری نباید خارج از ترتیب Roadmap وارد این زیرمرحله شود.

## 80. بسته‌شدن P1.5.3 و انتقال به P1.5.4

- وضعیت `P1.5.3`: بسته‌شده
- هدف: ثبت پایدار تاریخچه، ترتیب و اثرانگشت Migrationهای نسخه‌دار
- بسته هدف: `@workspace/database`
- Migration رسمی مرحله: `infra/migrations/0001_create-migration-history.sql`

خروجی‌های فنی:

- ایجاد idempotent جدول رسمی `public.orgawork_migration_history`
- ثبت نسخه، نام فایل، SHA-256، ترتیب اعمال و زمان اعمال هر Migration
- محاسبه اثرانگشت قطعی SHA-256 با خروجی lowercase شصت‌وچهارکاراکتری
- کنترل دقیق Prefix تاریخچه نسبت به Migrationهای کشف‌شده
- توقف پیش از اجرای SQL در صورت اختلاف نسخه، ترتیب یا اثرانگشت
- اجرای فقط پسوند Migrationهای جدید پس از تاریخچه معتبر
- ثبت Migration و رکورد تاریخچه در همان Transaction
- اجرای دوباره بدون تکرار Migrationهای ثبت‌شده
- خطاهای پایدار و بدون افشای SQL یا Credential
- پاک‌سازی کامل Artifactهای پذیرش واقعی

شواهد پذیرش:

- آزمون متمرکز تاریخچه Migration: `8/8` موفق
- Bootstrap جدول تاریخچه روی PostgreSQL واقعی: موفق
- ثبت ترتیب اعمال روی PostgreSQL واقعی: موفق
- ثبت و کنترل SHA-256 روی PostgreSQL واقعی: موفق
- اجرای دوباره و Skip نسخه‌های ثبت‌شده: موفق
- تشخیص تغییر اثرانگشت پیش از اجرای SQL: موفق
- پاک‌سازی Artifact پذیرش: موفق
- کنترل کامل پروژه: `24` فایل آزمون و `118/118` آزمون موفق
- ساخت چهار برنامه و چهار بسته وابسته: `8/8` Task موفق
- Worktree پس از ثبت: پاک

ثبت Git:

- Commit فنی: `9f41511f8fa8cc06bfb0a8272a6cdd5b882d9dd3`
- پیام Commit: `P1.5.3: add persistent migration history`
- فایل‌های Commit فنی: `4` فایل دقیق
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.
- انتقال رسمی: `P1.5.4 — ایجاد نقش‌های پایگاه داده با کمترین سطح دسترسی`

قید ادامه:

زیرمرحله `P1.5.4 — ایجاد نقش‌های پایگاه داده با کمترین سطح دسترسی` باید دقیقاً مطابق Roadmap اجرا شود. تغییرات مربوط به زیرمرحله‌های بعدی، Schema دامنه، RLS یا رابط کاربری نباید زودتر وارد دامنه شوند.

## 81. بسته‌شدن نهایی P1.5 و انتقال به P1.6

- مرحله مادر بسته‌شده: `P1.5 — مهاجرت پایگاه داده و جداسازی سازمانی`
- زیرمرحله‌های بسته‌شده در این اختتام: `P1.5.4` تا `P1.5.14`
- انتقال رسمی: `P1.6.1 — تکمیل بسته قراردادهای مشترک`

خروجی‌های فنی نهایی:

- نقش‌های Migration و Runtime با کمترین سطح دسترسی و بدون Login
- منع صریح عبور نقش‌های عادی از RLS
- زمینه سازمان محدود به Transaction و بدون نشت Pool
- فعال‌سازی و اجبار RLS برای زیرساخت سازمانی
- جداول Outbox، Inbox، Heartbeat و زمان‌های پایان امن
- بررسی انحراف تعداد، ترتیب، نام فایل و SHA-256 Migrationها
- اثبات اجرای دوباره بدون اعمال مجدد Migrationها
- آزمون واقعی جلوگیری از Read، Insert، Update و Delete بین‌سازمانی
- منع دسترسی سازمانی در نبود Tenant Context

شواهد پذیرش:

- آزمون متمرکز نهایی Migration، نقش و Tenant Runtime: موفق
- پذیرش واقعی PostgreSQL برای Drift، Rerun، NOBYPASSRLS و RLS: موفق
- کنترل کامل پروژه: موفق
- Build برنامه‌ها و بسته‌های وابسته: موفق
- Artifactهای پذیرش واقعی: پاک‌سازی‌شده
- Commit نقش‌های کمترین دسترسی: `dbbf0fb902a3016522e0fa92e4364968810b5843`
- Commit زیرساخت جداسازی سازمانی: `18df7c38bf58afd75c0fa7c422f6363f6b1ca412`
- Commit کنترل Drift و پذیرش نهایی: `0dbdf01003df2d1351991074c4e98d01f5c26bd8`
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.

قید ادامه:

مرحله `P1.6` باید مطابق Roadmap و بدون ورود زودهنگام به رابط کاربری اجرا شود.

## 82. بسته‌شدن P1.6 و انتقال به P1.7

- مرحله بسته‌شده: `P1.6 — قراردادهای مشترک و OpenAPI`
- زیرمرحله‌های بسته‌شده: `P1.6.1` تا `P1.6.12`
- انتقال رسمی: `P1.7.1 — تعریف ثبت رخداد ساختاریافته مشترک`

خروجی‌های فنی:

- بسته واقعی `@workspace/contracts` با نسخه قرارداد پایدار
- شناسه‌های نوع‌دار، زمان UTC و انواع پایه مشترک
- قالب استاندارد پاسخ موفق و خطاهای پایدار با پیام عمومی فارسی
- قراردادهای Pagination، Sort و Filter
- قرارداد زمینه نشست و سازمان
- سند OpenAPI 3.1 برای مسیرهای Health و Readiness
- مسیر واقعی `/ready` در Server
- بسته واقعی `@workspace/api-client` و مشتری نوع‌دار
- اثرانگشت SHA-256 سند OpenAPI در Client تولیدشده
- کنترل Drift میان OpenAPI، Server و Client
- آزمون سازگاری Client با مسیرهای واقعی Fastify

شواهد پذیرش:

- کنترل Contract Drift: موفق
- آزمون‌های متمرکز Contract، Client، Server و Drift: موفق
- Typecheck و Build بسته‌های جدید: موفق
- کنترل کامل پروژه: موفق
- Build برنامه‌ها: موفق
- Commit فنی: `eab8f0c1df4e3ff3fd1feba34cb4be7a9313b887`
- Tag اختصاصی از قبل تعریف نشده بود و ایجاد نشد.

قید ادامه:

مرحله `P1.7` باید بدون ورود زودهنگام به طراحی واقعی رابط کاربری اجرا شود.

## 83. بسته نخست P1.7 و انتقال به P1.7.5

- زیرمرحله‌های بسته‌شده: `P1.7.1` تا `P1.7.4`
- انتقال رسمی: `P1.7.5 — ایجاد سنجه‌های پایه رابط برنامه‌نویسی`

خروجی‌های فنی:

- بسته جدید `@workspace/observability`
- Structured Logger مشترک با خروجی JSON Line
- Request ID و Correlation ID معتبر و نسخه‌پذیر
- W3C Trace Parent، Trace ID، Span ID و Child Span
- Async Context مشترک با AsyncLocalStorage
- Redaction مرکزی برای Password، Secret، Token، Authorization، Cookie، API Key و URI Credential
- ثبت مستقیم Hookهای مشاهده‌پذیری روی نمونه اصلی API و پاسخ Headerهای درخواست و Trace
- استفاده Worker و Scheduler از Logger مشترک

پذیرش:

- آزمون‌های متمرکز مشاهده‌پذیری: موفق
- آزمون Request Header و Trace Header: موفق
- Lint، Typecheck و Build: موفق
- کنترل کامل پروژه: موفق
- Build مستقیم برنامه‌ها بدون Turbo: موفق
- Commit فنی: `5bb17a094ba2fa3dfc25fed565e404f676c403f7`
- Tag اختصاصی ایجاد نشد.

قید ادامه:

سنجه‌های API، پایگاه داده، صف و پردازشگر در بسته دوم P1.7 انجام می‌شوند. طراحی واقعی رابط کاربری همچنان خارج از دامنه است.

## 84. پذیرش نهایی P1.7 و انتقال به P1.8.1

- زیرمرحله‌های بسته‌شده: `P1.7.5` تا `P1.7.12`
- مرحله مادر بسته‌شده: `P1.7 — مشاهده‌پذیری و سلامت عملیاتی`
- انتقال رسمی: `P1.8.1 — ایجاد گردش کار یکپارچه‌سازی مداوم`

خروجی‌های فنی:

- Metric Registry و خروجی Prometheus
- سنجه‌های HTTP، Database، Transaction، Queue، Worker و Scheduler
- Liveness، Health جزئی و Metrics
- Heartbeat پردازشگر و زمان‌بند
- Dashboard و Alert پایه
- آزمون Failure و Recovery

پذیرش:

- آزمون‌های متمرکز، Lint، Typecheck و Build: موفق
- کنترل کامل پروژه و Build مستقیم: موفق
- Commit فنی: `c13015d62ed2d3d9141f1f9962428fea343dd33d`
- Evidence: `EVD-027`
- Tag اختصاصی ایجاد نشد.

قید ادامه:

مرحله بعد فقط P1.8 است و طراحی واقعی رابط کاربری همچنان آغاز نشده است.

## 85. پذیرش نهایی P1.8 و انتقال به P1.9.1

- زیرمرحله‌های بسته‌شده: `P1.8.1` تا `P1.8.14`
- مرحله مادر بسته‌شده: `P1.8 — یکپارچه‌سازی مداوم و آزمون‌های معماری`
- انتقال رسمی: `P1.9.1 — بررسی عملی مرز ماژول‌ها و جلوگیری از وابستگی چرخه‌ای`

خروجی‌های فنی:

- GitHub Actions چندسکویی
- Frozen Lockfile و وابستگی‌های نسخه‌ثابت
- Quality، Coverage، Build، Contract و Migration Gates
- Architecture و Dependency Policy
- Secret و UTF-8 Policy
- JUnit، Coverage و CI Artifact
- Branch Protection baseline بدون تغییر Remote

پذیرش:

- آزمون‌های متمرکز ابزارهای P1.8: موفق
- Coverage و تولید گزارش محلی: موفق
- کنترل کامل پروژه: موفق
- Build مستقیم برنامه‌ها بدون Turbo: موفق
- Commit فنی: `a25f76df004f418119159706540379d689e12d91`
- Evidence: `EVD-028`
- Tag اختصاصی ایجاد نشد.

قید ادامه:

مرحله P1.9 فقط بررسی فنی محدود و شاهدپذیر است. طراحی واقعی رابط کاربری همچنان آغاز نشده است.

## 86. پذیرش نهایی P1.9 و انتقال به P1.10.1

- زیرمرحله‌های بسته‌شده: `P1.9.1` تا `P1.9.8`
- مرحله مادر بسته‌شده: `P1.9 — بررسی‌های فنی اجباری`
- انتقال رسمی: `P1.10.1 — ممیزی همه مراحل P1.1 تا P1.9`

شواهد فنی:

- Architecture واقعی و آزمون‌های Cycle
- Outbox و Inbox واقعی با Duplicate rejection
- Redis Lease با NX، PX، انقضا و بازیابی
- Cache نشست و سازمان جاری با Invalidation
- RLS، Pool و Context هم‌زمان PostgreSQL
- محدودیت‌ها و روابط مرکب Catalog
- گزارش Machine-readable و Markdown Spike

پذیرش:

- آزمون‌های متمرکز Spike: موفق
- اجرای واقعی PostgreSQL و Redis: موفق
- کنترل کامل پروژه: موفق
- Build مستقیم برنامه‌ها: موفق
- Commit فنی: `b4d3ff0a853fe2b8d4e52bba05862bf0fb46b228`
- Evidence: `EVD-029`
- Tag اختصاصی ایجاد نشد.

قید ادامه:

P1.10 باید پذیرش نهایی کل P1 را انجام دهد. طراحی واقعی رابط کاربری همچنان آغاز نشده است.

## 87. پذیرش نهایی بنیاد P1 و انتقال به P2.1

- زیرمرحله‌های بسته‌شده: `P1.10.1` تا `P1.10.11`
- مرحله مادر بسته‌شده: `P1.10 — پذیرش نهایی پایه پروژه`
- فاز بسته‌شده: `P1 — بنیاد فنی و اجرایی`
- انتقال رسمی: `P2.1 — تثبیت قرارداد دامنه هویت و سازمان`

پذیرش واقعی:

- ممیزی شواهد، Commitها و Tagهای P1: موفق
- نصب پاک با Frozen Lockfile در Git Worktree جدا: موفق
- کنترل کامل و Coverage: موفق
- ساخت تازه چهار برنامه بدون Cache قبلی: موفق
- اجرای رسمی `infra:start` بدون Attach مسدودکننده: موفق
- Migration و RLS واقعی در دو اجرای متوالی: موفق
- Smoke هماهنگ چهار برنامه: موفق
- ممیزی مستندات و Traceability: موفق

ثبت:

- Commit فنی: `728993f240296a8017cb3fb883c3c7802fcba575`
- Evidence: `EVD-030`
- Tag: `stage-p1.10-foundation-acceptance-complete`

مرز ادامه:

قابلیت‌های دامنه از P2 آغاز می‌شوند. طراحی واقعی رابط کاربری تا P2.13 آغاز نمی‌شود و پیش از آن توقف و اطلاع به کاربر الزامی است.

# P2 — بسته‌شدن هویت سازمان و مجوزدهی

## نتیجه نهایی

- `P2.4` تا `P2.15` در بسته مادر کنترل‌شده اجرا و پذیرفته شدند.
- Argon2id با پروفایل Benchmark شده `32 MiB / 3 / 1` ثبت شد.
- نشست Server Side، محدودسازی ورود، خروج، خروج همگانی، بازیابی گذرواژه و CSRF پیاده‌سازی شدند.
- سازمان جاری، نقش‌ها، قواعد رابطه‌ای و وضعیت منبع، Explicit Deny، دعوت، عضویت و تیم‌ها تکمیل شدند.
- رابط فارسی RTL با Vazirmatn و Proxy سمت سرور Web به API تکمیل شد.
- Migrationهای `0005` تا `0009`، آزمون‌های منفی و پذیرش واقعی PostgreSQL اجرا شدند.
- شاهد نهایی: `EVD-041`.
- مرحله بعد: `P3.1`.

# P2R.1.1 — ثبت خط‌مبنای اصلاح بنیاد رابط

## علت

پس از بسته‌شدن P2 مشخص شد رابط P2.13 مسیرها و رفتار پایه را پیاده‌سازی کرده، اما از معماری سه مرجع منجمد به شکل مورد انتظار استفاده نکرده است. همچنین تصمیم گرفته شد توسعه قابلیت‌ها فعلاً با متن English انجام شود و ترجمه کامل فارسی نزدیک پایان پروژه تکمیل شود.

## ممیزی ورودی

- HEAD آغاز: `0be4eb3e1dcf63c358ed9a2751103d4d410eb30b`
- Worktree آغاز: پاک
- `Studio Admin`: Commit منجمد و مجوز MIT تأیید شد.
- `TailAdmin Next.js`: Commit منجمد و مجوز MIT تأیید شد.
- `Kiranism Dashboard`: Commit منجمد و مجوز MIT تأیید شد.
- وب فعلی فاقد زیرساخت استاندارد i18n، Theme Provider و مؤلفه‌های مرجع بود.
- فایل‌های فعلی به `fa` و `rtl` ثابت و متن‌های مستقیم فارسی وابسته بودند.

## خروجی

- مسیر مستقل `P2R` پیش از P3 ثبت شد.
- `DEC-UI-002` پذیرفته شد.
- `UI-BASELINE-P2R-001` ثبت شد.
- P2 بسته و محفوظ باقی ماند.
- مرحله بعد به `P2R.1.2` منتقل شد.
- P3.1 هنوز آغاز نشده است.

# P2R.1.2 — ایجاد زیرساخت چندزبانه و جهت پویا

## پیاده‌سازی

- `next-intl` نسخه ثابت `4.13.4` به برنامه Web افزوده شد.
- فایل Request Config و Plugin رسمی Next.js ایجاد شد.
- کاتالوگ‌های `en` و `fa` با ساختار یکسان ایجاد شدند.
- زبان توسعه بدون Cookie برابر `en` است.
- زبان پیش‌فرض محصول نهایی همچنان `fa` ثبت شده است.
- جهت سند برای English برابر `ltr` و برای فارسی برابر `rtl` است.
- انتخاب زبان در Cookie اختصاصی HttpOnly با SameSite=Lax نگهداری می‌شود.
- تعویض زبان بدون وابستگی به نشست یا احراز هویت انجام می‌شود.
- متن‌های قابل مشاهده پنج صفحه موجود از Componentها به کلیدهای ترجمه منتقل شدند.
- فونت محلی Vazirmatn فقط برای سند فارسی فعال می‌شود.
- امنیت نشست و CSRF و مسیرهای API هویت بدون تغییر باقی ماندند.

## دروازه‌ها

- برابری ساختاری کاتالوگ‌ها
- آزمون بنیاد i18n
- آزمون‌های هدف P2
- Typecheck برنامه Web
- Build تولیدی برنامه Web
- Formatting و Diff Integrity

## خروجی

- `P2R.1.2` بسته شد.
- مرحله جاری به `P2R.1.3` منتقل شد.
- `P3.1` آغاز نشد.

# P2R.1.3 — سیستم طراحی و مؤلفه‌های پایه

## پیاده‌سازی

- جای‌نگهدار `packages/ui/.gitkeep` به Workspace واقعی `@workspace/ui` تبدیل شد.
- بسته UI با Build TypeScript و Exportهای Source برای توسعه Next.js ایجاد شد.
- توکن‌های معنایی روشن و تیره به‌صورت CSS Variables و Tailwind Theme ثبت شدند.
- مؤلفه‌های Button Card Input Label Separator Badge Tooltip ایجاد شدند.
- Theme Provider و Theme Toggle با `next-themes` ایجاد شدند.
- Primitiveهای دسترس‌پذیر از `radix-ui` و آیکن‌ها از `lucide-react` استفاده می‌کنند.
- هیچ متن محصولی در بسته UI هاردکد نشد.
- برنامه Web وابستگی `@workspace/ui` و `transpilePackages` را ثبت کرد.
- کلاس‌های بسته UI با دستور `@source` در Tailwind نسخه 4 قابل شناسایی شدند.
- کنترل زبان و کنترل پوسته در Provider مشترک برنامه ادغام شدند.
- امنیت نشست احراز هویت CSRF و Backend بدون تغییر باقی ماندند.
- فرمان تاریخی `build:foundation:direct` بدون تغییر حفظ شد و ساخت بسته UI جداگانه انجام شد.
- ممیزی نهایی P2 به‌صورت سازگار با گذشته هم `dir="rtl"` ثابت و هم جهت پویا بر پایه Locale را می‌پذیرد.

## دروازه‌ها

- آزمون مؤلفه‌ها و Utilityهای بسته UI
- آزمون بنیاد سیستم طراحی
- آزمون‌های مستندات و زیرساخت چندزبانه
- آزمون‌های محفوظ P2
- Typecheck و Build بسته UI
- Lint بسته UI و برنامه Web
- Typecheck و Production Build برنامه Web
- Architecture Policy و Repository Security
- مجموعه کامل آزمون‌ها
- Formatting و Diff Integrity

## خروجی

- `P2R.1.3` بسته شد.
- مرحله جاری به `P2R.1.4` منتقل شد.
- `P3.1` آغاز نشد.

# P2R.1.4 — پوسته واکنش‌گرای داشبورد

## پیاده‌سازی

- `DashboardShell` به‌عنوان پوسته مشترک صفحه اصلی ایجاد شد.
- نوار کناری با مسیر فعال، آیکن، برند داخلی OrgaWork و وضعیت عضویت ایجاد شد.
- سربرگ Sticky شامل سازمان جاری، تغییر سازمان، تغییر زبان و تغییر Theme ایجاد شد.
- نوار کناری در موبایل به‌صورت Off-canvas همراه Backdrop اجرا می‌شود.
- بستن با Escape، بستن پس از انتخاب لینک و قفل موقت Scroll پیاده‌سازی شد.
- `ApplicationFrame` کنترل‌های زبان و Theme را برای صفحات خارج از داشبورد حفظ می‌کند.
- صفحه داشبورد از مؤلفه‌های Card بسته `@workspace/ui` استفاده می‌کند.
- تمام متن‌های پوسته در کاتالوگ English و فارسی ثبت شدند.
- آیکن‌های مورد نیاز از API کنترل‌شده بسته `@workspace/ui` صادر شدند.
- هیچ برند، تبلیغ، داده یا Backend قالب‌های مرجع وارد پروژه نشد.
- Backend، امنیت و خط‌مبنای بسته‌شده P2 بدون تغییر باقی ماندند.

## دروازه‌ها

- کنترل Commit و Worktree
- کنترل Commit و مجوز سه مرجع منجمد
- کنترل نبود مسیر نسبی `.js` در سورس UI
- کنترل نبود برند و وابستگی ممنوع
- آزمون متمرکز پوسته داشبورد
- آزمون‌های محفوظ P2
- Typecheck و Lint بسته UI و برنامه Web
- Build بسته UI و Production Build برنامه Web
- Architecture Policy و Repository Security
- مجموعه کامل آزمون‌های مخزن
- Formatting و Diff Integrity

## خروجی

- `P2R.1.4` بسته شد.
- مرحله جاری به `P2R.1.5` منتقل شد.
- `P3.1` آغاز نشد.

# P2R.1.5 — ورود بازیابی و انتخاب سازمان

## پیاده‌سازی

- صفحه ورود با `AuthPageShell` و مؤلفه‌های بسته `@workspace/ui` بازطراحی شد.
- پنل بصری بدون برند تبلیغاتی و فقط با متن‌های داخلی OrgaWork ایجاد شد.
- نمایش و پنهان‌سازی گذرواژه با `PasswordField` دسترس‌پذیر پیاده‌سازی شد.
- مسیرهای درخواست و تأیید بازیابی گذرواژه ایجاد شدند.
- لینک بازیابی صفحه ورود دیگر به مسیر ناموجود اشاره نمی‌کند.
- صفحه انتخاب سازمان داده نشست و سازمان‌ها را به‌صورت هم‌زمان دریافت می‌کند.
- سازمان جاری از `currentOrganizationId` واقعی تشخیص داده می‌شود.
- تغییر سازمان همچنان فقط با `x-csrf-token` نشست واقعی انجام می‌شود.
- وضعیت‌های Loading Error Empty Current و Submitting ایجاد شدند.
- تمام متن‌های قابل مشاهده در کاتالوگ English و فارسی ثبت شدند.
- آیکن‌های مورد نیاز از API کنترل‌شده `@workspace/ui` صادر شدند.
- هیچ ورود اجتماعی Clerk داده نمایشی یا Backend قالب‌ها وارد نشد.
- خط‌مبنای بسته‌شده P2 و فرمان تاریخی P1 بدون تغییر حفظ شدند.

## دروازه‌ها

- کنترل Commit و Worktree
- کنترل Commit و مجوز سه مرجع منجمد
- کنترل ثابت‌ماندن فرمان تاریخی `build:foundation:direct`
- کنترل نبود مسیر نسبی `.js` در سورس UI
- کنترل نبود ورود اجتماعی داده نمایشی و Browser Storage
- کنترل قرارداد Login Password Reset Session Organization و CSRF
- آزمون متمرکز ورود بازیابی و انتخاب سازمان
- آزمون‌های محفوظ P2
- Typecheck Lint و Build بسته UI و برنامه Web
- Architecture Policy و Repository Security
- مجموعه کامل آزمون‌های مخزن
- Formatting و Diff Integrity

## خروجی

- `P2R.1.5` بسته شد.
- مرحله جاری به `P2R.1.6` منتقل شد.
- `P3.1` آغاز نشد.

# P2R.1.6 — اعضا دعوت‌ها و تیم‌ها

## پیاده‌سازی

- صفحات اعضا و تیم‌ها داخل `DashboardShell` قرار گرفتند.
- `ApplicationFrame` کنترل‌های تکراری زبان و Theme را در مسیرهای داشبورد حذف می‌کند.
- درخواست خواندن اعضا و تیم‌ها با `x-csrf-token` نشست واقعی اصلاح شد.
- فرم دعوت با نوع‌سنجی صریح مقادیر `FormData` پیاده‌سازی شد.
- نتیجه دعوت و پیوند توسعه فقط در صورت بازگشت واقعی Token نمایش داده می‌شود.
- صفحه `invitations/[token]` برای پذیرش امن دعوت ساخته شد.
- `MemberAccessEditor` تغییر وضعیت و چند نقش سازمانی را مدیریت می‌کند.
- حداقل یک نقش هنگام ذخیره نقش‌ها الزامی باقی می‌ماند.
- صفحه تیم‌ها ایجاد جست‌وجو و تغییر نام تیم را با `TeamRenameForm` ارائه می‌کند.
- جدول اعضا در نمایش موبایل به کارت‌های خوانا تبدیل می‌شود.
- تمام متن‌های قابل مشاهده در کاتالوگ English و فارسی ثبت شدند.
- هیچ Backend قرارداد Migration یا سیاست امنیتی P2 تغییر نکرد.
- خط‌مبنای بسته‌شده P2 و فرمان تاریخی P1 بدون تغییر حفظ شدند.

## دروازه‌ها

- کنترل Commit و Worktree
- کنترل Commit و مجوز سه مرجع منجمد
- کنترل ثابت‌ماندن فرمان تاریخی `build:foundation:direct`
- کنترل نبود مسیر نسبی `.js` در سورس UI
- کنترل نبود Template Brand Demo Data و Browser Storage
- کنترل قراردادهای Session CSRF Membership Invitation Role Team و Rename
- آزمون متمرکز اعضا دعوت‌ها و تیم‌ها
- آزمون‌های محفوظ P2
- Typecheck Lint و Build بسته UI و برنامه Web
- Architecture Policy و Repository Security
- مجموعه کامل آزمون‌های مخزن
- Formatting و Diff Integrity

## خروجی

- `P2R.1.6` بسته شد.
- مرحله جاری به `P2R.1.7` منتقل شد.
- `P3.1` آغاز نشد.

# P2R.1.7 — دسترس‌پذیری واکنش‌گرایی Theme و جهت صفحه

- Commit مبنا: `81e4fd8826309301b4d21dbc9b472ba65a89e4d1`
- آزمون مرورگر واقعی با `@playwright/test` و Google Chrome از طریق `channel: chrome` اضافه شد
- ممیزی خودکار `@axe-core/playwright` برای قواعد WCAG 2.0 و 2.1 در سطوح A و AA اضافه شد
- مسیرهای اصلی رابط در English و فارسی، حالت روشن و تیره و اندازه‌های دسکتاپ و موبایل بررسی می‌شوند
- لینک پرش مستقل برای صفحات تمام‌عرض و داشبورد قابل تمرکز شد
- تمرکز پس از بازشدن منوی موبایل به دکمه بستن منتقل و پس از Escape به دکمه منو بازگردانده می‌شود
- عنوان صفحه پذیرش دعوت به `h1` معنایی تبدیل شد
- انیمیشن Spinner و Transition کارت‌ها در حالت `prefers-reduced-motion: reduce` متوقف می‌شوند
- سرریز افقی در عرض‌های 360 و 1440 پیکسل برای همه مسیرهای اصلی کنترل می‌شود
- داده‌های نشست، سازمان، اعضا و تیم‌ها در آزمون مرورگر فقط در مرز شبکه مرورگر Mock می‌شوند و Backend پروژه تغییر نمی‌کند
- خط‌مبنای امنیتی P2 و ممنوعیت ذخیره نشست در Browser Storage حفظ شد
- مرحله بعد: `P2R.1.8 — ساخت تولیدی بازگشت کامل آزمون‌ها و پذیرش اصلاح رابط`

# P2R.1.8 — ساخت تولیدی بازگشت کامل آزمون‌ها و پذیرش اصلاح رابط

## Commit مبنا

- `37bd82c2e9cba0a81b30e761dd8cb679d77a62fa`
- `P2R.1.7: add accessibility and responsive browser gates`

## دامنه

- هیچ قابلیت جدیدی از `P3.1` پیاده‌سازی نشد.
- هیچ Backend، قرارداد، Migration، نشست، CSRF یا سیاست امنیتی `P2` تغییر نکرد.
- تغییرهای مرحله به اسناد پذیرش نهایی، آزمون همگام‌بودن مستندات، اصلاح محدود ابزارهای مرورگر و به‌روزرسانی معیار منسوخ Smoke برای سند دوزبانه محدود شدند.

## دروازه‌ها

- نصب منجمد تمام Workspaceها
- Formatting کل مخزن
- TypeScript کل مخزن، Lint صفر برای دامنه `P2R` و ممیزی دقیق خط مبنای ۵۸ خطای قدیمی بدون خطای جدید
- Architecture Policy و Repository Security
- Contract Drift
- ساخت تولیدی تازه تمام Workspaceهای دارای Build با `turbo run build --force` و ثبت Checkpoint قابل‌اعتبارسنجی
- آزمون واقعی Google Chrome با Playwright و Axe و استفاده مجدد فقط از Checkpoint مقید به همان HEAD
- Smoke هماهنگ برنامه‌های اجرایی با پذیرش زوج معتبر `en/ltr` یا `fa/rtl`
- مجموعه کامل آزمون‌های مخزن
- کنترل Diff Whitespace و دامنه مسیرها

## نتیجه

- شاهد `EVD-042` ثبت شد.
- گزارش `docs/acceptance/P2R-FINAL-ACCEPTANCE.md` ایجاد شد.
- مسیر اصلاحی `P2R` بسته و پذیرفته شد.
- `P3.1` در این Commit آغاز نشد.
- آغاز `P3.1` به گام جداگانه بعدی منتقل شد.
- Tag جدید ایجاد نشد.

# P34.C1 — قرارداد مشترک P3.1 و P4.1

## دامنه

- قرارداد Aggregate پرونده در `modules/cases` تثبیت شد.
- قرارداد چرخه مسئولیت و مسئولیت اصلی فعال یکتا در `modules/assignments` تثبیت شد.
- قرارداد اقدام اصلی و فرعی و ثبت اتمی نتیجه و ادامه در `modules/actions` تثبیت شد.
- قرارداد انتظار داخلی و خارجی، مانع، توقف موقت، ماشین انتقال و تصمیم نسخه‌دار در `modules/followup-state` تثبیت شد.
- شناسه‌ها و انواع مشترک P34 در `packages/contracts` افزوده شدند.
- سند `docs/contracts/P3.1-P4.1-CASE-FOLLOWUP-CONTRACT.md` منبع قرارداد این Checkpoint است.

## مرز

- هیچ Migration یا جدول پایگاه داده ایجاد نشد.
- هیچ Route اجرایی API، رابط کاربری، Worker یا Scheduler تغییر نکرد.
- P5 وارد بسته P34 نشد.

## دروازه‌ها

- آزمون‌های متمرکز پنج Workspace قرارداد
- TypeScript، Lint و Build متمرکز
- Architecture Policy و Repository Security
- Contract Drift
- Formatting و Diff Integrity
- Commit واقعی و Worktree پاک

## خروجی

- `P3.1` و `P4.1` در یک Checkpoint مشترک ولی با مالکیت مستقل تثبیت شدند.
- مرحله مجاز بعدی `P34.C2 — مدل داده، Migration و جداسازی` است.
- Migration بعدی باید از `0010` آغاز شود.

> **Clarification تاریخی (پسین):** checkpoint `P34.C1` یک رویداد تاریخی است و برای مدل اجرایی مرحله‌ای پروژه به دو مالکیت مستقل تفکیک شد: بخش `P3.1` → مالکیت **P3** (وضعیت: پذیرفته‌شده)، بخش `P4.1` → مالکیت **P4** (وضعیت: تا پایان کامل P3 مسدود). `P34` دیگر مدل اجرایی فعلی پروژه نیست و ترتیب رسمی از این پس `P2 → P3 → P4 → P5 → ...` است. ارجاعات `P34.C2`/`P34.C3` و شماره Migration `0010` در این بخش صرفاً یادداشت آن زمان هستند و در برنامه‌ریزی آینده بازنگری خواهند شد.
